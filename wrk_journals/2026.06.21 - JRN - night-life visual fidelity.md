# JRN — mdtrain2 night-life visual fidelity

**Task:** integrate the handoff HLD `wrk_docs/2026.06.21 - HLD - mdtrain2 night-life visual fidelity.md`
(another agent's draft + a working A/B visual proof, delivered as `Game visual improvement strategy.zip`).
**Worktree:** `c:/worktrees/mdtrain2/ai-render` · branch `task/night-visual` off `origin/master` @ `6e35e0c`.
**Version:** 0.13.1 → 0.14.0 (one bump at the end).

## Decision (how to integrate)
Accept the HLD. It is **100% procedural** — no new npm dep, no committed assets, **no change to
`src/sim/*`** — so the 320 oracles stay green by construction; risk is visual/perf only. Land it as a
slice-by-slice campaign in the §7 order (cheapest-impact first), gating **each** slice with a
night-time Playwright screenshot + `npm run check`. Keep the pure core untouched; unit-test the only
new pure surface if any (a `quality.ts` tier field / optional `celestialDir`). **Hold deployment until
Arthur eyeballs the look** (R5: taste needs human eyes). The proof (`.dc.html`) is the look/technique
source of truth; every `_frame(u)` becomes `placeOnCentreline(route,s,d)` + `anchorY`/`formationHeight`.

Slices: 1 lit-window facades · 2 UK signal heads (geometry) · 3 celestial layer + denser lamps ·
4 truss bridge · 5 traffic (optional).

## Slice 1 — lit-window facades ✅ (verified, screenshot-gated)
- `textures.ts`: new exported `buildFacade(aniso, seed)` → `{albedo, emissive}` — a 128×256 window-grid
  canvas; albedo = mid-grey masonry + dark panes (so the per-instance tint reads), emissive = ~50% of
  windows lit warm. Both sRGB-pinned (D23). One shared map (DL1); `seed` reserved for future variants.
- `scenery.ts` `buildBuildings`: wire `map`/`emissiveMap`/`emissive:0xffffff`/`emissiveIntensity:1.2`
  onto the single building InstancedMesh; `instanceColor` still tints per block. Same massing/placement.
- **Result:** the Kingsgate skyline now reads as a lit city at night — windows bloom (AC-A night half).
  `tsc` clean, 320 tests pass. Box UVs stretch the grid per block (R2, accepted at night/speed).
- **Open:** AC-A bright-day washout to confirm with the focus-fixed harness; `emissiveIntensity`
  tunable. The proof's tints matched scenery.ts's, so colours are on-brand.

## Slice 2 — UK signal heads (geometry only) ✅ implemented + tested (visual-confirm pending)
- `scene.ts buildSignals`: head geometry rebuilt to UK pattern — spherical lamps recessed in short
  open black hoods on the existing black backboard, **RED at the BOTTOM**. Top→bottom order:
  amberBot (upper yellow, DOUBLE_YELLOW only), green, amberTop (lower yellow, YELLOW+DOUBLE_YELLOW),
  red. **Aspect WIRING UNCHANGED** — same red/amberTop/amberBot/green materials `updateSignals`
  drives from the pure core (AC-B logic stays green: 320 tests pass, tsc clean). 06-17 DL4 preserved.
- **Visual confirm BLOCKED by snap flakiness:** the headless screenshots started timing out on the
  signal seeds (the game's perpetual rAF loop stalls Playwright's screenshot stability-wait — the
  exact gotcha the Coil harness documents) and earlier failed runs left **22 orphaned chromium procs**
  hogging resources. The post renders; the head close-up isn't captured cleanly yet. Decision: STOP
  fighting ad-hoc snaps — **adopt the Coil `@playwright/test` harness** (webServer + `animations:
  "disabled"` + error-tolerant `shot()`) before slices 3–5, then batch the slice-1/2/… visual gates
  through it reliably. (snap.mjs hardened: domcontentloaded + animations-disabled + try/catch, but the
  seed-specific timeout persists → harness is the real fix.)

## Tooling note
Reusing the ai-render worktree's Playwright loop (`scripts/snap.mjs` now cycles weather via `e` for
night). Planning to adopt the Coil agent's `@playwright/test` harness pattern (focuses `#app` before
keys — fixes the reverser/`e` non-registration) so I can do moving night fly-throughs + assert no
console errors as the per-slice gate.
