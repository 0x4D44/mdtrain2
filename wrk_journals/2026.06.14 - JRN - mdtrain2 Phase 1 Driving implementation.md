# 2026.06.14 — JRN — mdtrain2 Phase 1 (Driving) implementation

Lead: Claude (autonomous). Implementing the SIGNED-OFF HLD:
`wrk_docs/2026.06.14 - HLD - mdtrain2 Phase 1 Driving.md`.

## Kickoff decisions (team lead)
- **Precondition check passed:** HLD present and carries `Status: SIGNED-OFF`. Repo at HEAD `eb482d5`, working tree clean.
- **Path:** unambiguous — the single signed-off HLD in this repo, authored this session. No clarifying questions were blocking.
- **Commits:** complete, passing units committed locally as we go. **No push** (no push directive; standing rule). Author `0x4D44`.
- **Budget:** no `+500k` directive → default proportionality; lean fan-out.
- **Definition of done:** the HLD's acceptance oracles O1–O23 + `npm run check` green (tsc --noEmit && vitest run), no wall-clock under `src/sim`, `main.ts` holds no control/safety/HUD arithmetic.

## Phase log
### Phase 1 — VALIDATE & DRIFT CHECK  ✓
- 3 parallel readers verified the HLD's sim-core APIs, test harness/strict-TS, and route/train constants against HEAD `eb482d5`.
- **Drift verdict: NONE** (0 fundamental). 3 "items" flagged are not drift — they are the planned edits the HLD owns (no `emergency` field yet; version still 0.1.0). `npm run check` green at HEAD (11 tests).
- O21 force differential recomputed non-vacuous: grade 0.12 @ μ=0.2 → gravity 188,288 N strictly between service-hold 155,520 N and emergency-hold 224,640 N (adhesion 313,813 N non-binding).
- Acceptance criteria O1–O23 + global gates extracted → `.impl-work/validation-report.md` (build's definition of done).

### Phase 2 — PLAN  ✓ (authored by lead; proportionality — plan is direct from the HLD impact map)
Three work items in strict dependency order (sequential; no file overlap, so no worktree isolation needed):
- **WI-1 — Physics emergency channel.** `physics.ts` (`brakeForce` gains `emergency=false`; `AccelInputs.emergency`; `acceleration` threads it to both brakeForce calls incl. static-hold) + `simulation.ts` (`SimInputs.emergency` required; `step` threads it). Update `test/physics.test.ts` literals (`emergency:false`). New emergency oracles O1, O2, O21 (+ regression green). DoD: `npm run check` green.
- **WI-2 — Controls/safety brain.** NEW `src/sim/controls.ts` (types, constants, `reduceControls`, `tickSafety`, `resolveInputs`, `buildHudView`, interlocks, shared penalty latch) + NEW `src/sim/aws.ts` stub. NEW `test/controls.test.ts` covering O3–O20, O22, O23, O8a/b, O9–O13, O14, O18, O19. Depends on WI-1.
- **WI-3 — Impure shell.** NEW `src/ui/hud.ts` (thin `createHud` DOM writer) + REWRITE `src/main.ts` (inline keymap, edge set, rAF + clamped dt, the §2.10 pipeline) + `package.json` 0.1.0→0.2.0. Untested glue (manual/build verification). Depends on WI-1, WI-2.

Commit one passing unit per work item. One implementer per item (sequential).

### Phase 3 — IMPLEMENT  ✓ (3 work items, dependency-ordered single implementers, lead verify+commit between)
**Decision (journaled):** realized IMPLEMENT as a sequential chain of single implementers (not a parallel pipeline) because the work items have hard import dependencies (controls.ts needs WI-1's `SimInputs.emergency`; main.ts needs WI-2) and share `npm`/tsc state — parallel would race and could not produce per-unit-passing commits. One implementer per item; lead ran the authoritative `npm run check` and committed each passing unit.

- **WI-1 — physics emergency channel** → commit `9d6b28d`. Deviation (journaled): `SimInputs.emergency` is required, which would break the Phase-0 `main.ts`; applied a 1-line interim `emergency:false` patch to main.ts to keep the unit green (main.ts fully rewritten in WI-3). Oracles O1/O2/O21 in new `test/emergency.test.ts`. check green (15 tests).
- **WI-2 — controls/safety brain** → commit `11a2901`. **Incident:** the first implementer hit an API socket error mid-run after writing `controls.ts` (~13 KB) + `aws.ts` but before tests/verify. **Recovery:** dispatched a fresh implementer to validate the orphaned source against the HLD (found it faithful — no corrections) and write the full `test/controls.test.ts`. Oracles O3–O20, O22, O23. check green (59 tests); determinism grep clean (no Date/random/perf.now/timers under src/sim).
- **WI-3 — impure shell** → commit `e61bd03`. New `src/ui/hud.ts` + main.ts rewrite (§2.10 pipeline) + version 0.1.0→0.2.0. check + `npm run build` green; main.ts holds no control/safety arithmetic (grep-verified). Headless smoke: spawn HUD correct (FULL SERVICE, next stop Riverside), no-power-against-brake interlock holds, train drives on brake release, zero console errors.

State after IMPLEMENT: `npm run check` green — 3 test files, 59 tests; all 23 oracles present (O1–O23).

### Phase 4 — REVIEW  ✓ (multi-dimension + 2-of-3 adversarial verification; looped to clean)
- **Round 1** (correctness / HLD-fidelity / tests / style): 0 critical; **2 verified majors** (both test-coverage, production code correct) + 6 minors.
  - F1: O9/O12/O23 built penalty fixtures from the FULL-brake spawn lever, masking the penalty term — **mutation-verified** (dropping `penaltyBrake` to 0 left them green). Rebuilt from a RELEASE lever.
  - F2: the acknowledge-**edge** half of latch-release was unpinned (O11 only had the stand-gate half). Added O10b.
  - Minors F3–F8: O20 isolation, `atStand()` dedupe, `resolvedBrakeDemand` helper (drop the `resolveInputs(c,s,0)` mu-hack), explicit dsd hold, hud lamp order, brakeLabel constants. → commit `8405baa` (60 tests).
- **Round 2** (confirmation): **CLEAN** — 0 critical, 0 major. 6 minors only. Applied 3 HLD-fidelity ones: F1 O21 secondary on-route both-hold check; F2 O14 limit path via `currentSpeedLimit` (P7); F3 remove the inert `else` in tickSafety. → commit `36bc754` (61 tests).
  - **Accepted as-is (journaled judgement):** O8a's `dsdWarning` end-equality sub-assertion is non-discriminating and O8b probes ±1 s rather than the exact boundary (both cosmetic — the load-bearing `vigilanceTimer`/Set equality and ordering ARE pinned; O8b covers crossings); and a same-frame `reverser+powerUp` is accepted (benign, faithful to the HLD's per-edge stale-read convention). No code change.
- REVIEW converged: a round came back clean and all acceptance criteria pass.

## Close-out
- **Shipped:** Phase 1 (Driving) implemented per the SIGNED-OFF HLD across WI-1/2/3 + 2 review commits (`9d6b28d`, `11a2901`, `e61bd03`, `8405baa`, `36bc754`).
- **Acceptance gate GREEN:** `npm run check` = 3 test files, **61 tests**, tsc zero warnings; `npm run build` clean; no `Date`/`Math.random`/`performance.now`/timers under `src/sim`; `main.ts` holds no control/safety/HUD arithmetic. All oracles O1–O23 implemented (O1/O2/O21 physics; O3–O20/O22/O23 controls; +O10b).
- **Deviations from the HLD:** none material. One interim step journaled (WI-1's 1-line `emergency:false` patch to main.ts, superseded by the WI-3 rewrite).
- **Parked:** nothing.
- **Not pushed** (no push directive; standing rule) — local commits only on `master`.
