# 2026.06.15 — JRN — mdtrain2 Phases 4 (World) & 5 (Reach)

Lead: Claude (full autonomous authority, incl. self-sign-off — Arthur granted
"design+implement in one run" + "full autonomy" for both phases).

## Kickoff (team lead)

**Mandate.** Arthur invoked the implement harness for "phase 4 then straight into
phase 5," but **no signed-off HLD existed** for either (only Phases 1-3 HLDs +
the PLN are in `wrk_docs/`). I stopped per the PRECONDITION and asked. Arthur
chose **"Design+implement in one run"** (collapse the two-step ceremony; I author
each HLD and self-sign-off under the delegated authority) and **scope "as written
in the plan"**:
- **Phase 4 — World:** route richness, station layouts, scenery, the wet-night
  **weather *system*** (Phase 3 shipped a *fixed* wet-night; the selectable
  day/time + weather system was explicitly deferred to Phase 4 in the P3 HLD's
  non-goals).
- **Phase 5 — Reach:** touch/mobile input, accessibility, performance pass.

**Proportionality decision (SCALE directive + Phase-3 precedent).** Both phases
are predominantly presentation/content/platform work with **low design-risk** and
mostly-untestable output (Three.js geometry, DOM, device polling). The Phase-3
journal already established the right shape for this: **author the HLD directly,
carve out the one or two genuinely *pure & testable* seams, self-sign-off, and put
the effort into implementation + build/screenshot verification + review.** The
heavy 3-draft/judge/Codex design cycle that suited Phases 1-2's load-bearing
invariants (the shared penalty latch, the signalling machine) is disproportionate
here. So:
- **VALIDATE/DRIFT:** I authored both HLDs against **current HEAD** (files read
  directly this session: `main.ts`, `route.ts`, `scene.ts`, `cab.ts`, `train.ts`,
  `package.json`). Drift is **nil by construction** — the design targets the tree
  as it actually is (v0.4.0, 111 tests, render seam = `RenderView`, `mu` hardwired
  `ADHESION.wetNight`, no `src/input/` yet). No separate drift workflow: re-reading
  my own just-read assumptions is exactly the "width has diminishing returns" the
  directive warns against. Acceptance criteria are extracted into each HLD §4.
- **IMPLEMENT:** Workflow pipeline, one implementer per work item, **pure tested
  seams partitioned by file from impure adapters**. Impure Three.js/DOM that needs
  a screenshot loop an agent can't run: the agent produces the first cut (DoD =
  tsc/build clean + integrates), then I (lead) own the build+screenshot verify and
  fix any rendering bug in a follow-up commit — exactly how Phase 3's WI-2 ran
  (agent draft → lead `−Z` cab fix).
- **REVIEW:** Workflow, small multi-dimension panel (correctness / tests / fidelity
  to HLD / security-lite / style) with 2-of-3 adversarial verification of each
  finding; loop until a clean round.

**The load-bearing testable seams I carved (so these aren't pure-aesthetic phases):**
- **Phase 4:** weather is tied to **adhesion μ** *and* visuals via a pure
  `environmentParams(env)` — wet conditions lower μ (feeds the real physics), night
  darkens lighting, rain drives particle intensity. Default (night+rain) pins
  μ = 0.20 to **preserve Phase 0-3 physics exactly** (differential/no-regression).
- **Phase 5:** a device-agnostic **input model** — `intentFromActions` /
  `mergeActions` — so keyboard, gamepad and touch all map through one pure, tested
  function to `ControlIntent`; plus pure `accessibilityParams` (reduced-motion
  gate) and `qualityFor` (perf scaling).

## Verification honesty
Autonomous verification reaches: `npm run check` green (pure oracles + the 111 sim
tests), `npm run build` clean, headless screenshots (wet-night stations/scenery;
env-cycle lighting change; narrow-viewport touch controls), zero console errors.
**Aesthetic quality (does the world/station look good? do the touch controls feel
right on a real phone?) needs human eyes** — flagged for the morning report.

---

## Phase log

### PHASE 4 (World) — design ✓
Authored `wrk_docs/2026.06.15 - HLD - mdtrain2 Phase 4 World.md`, self-signed.
Committed `9906157` (HLD+journal) + `ff566c8` (pinned the μ model to exact round
numbers so ENV4's night×rain = 0.25×0.8 = 0.20 = `ADHESION.wetNight` holds to 1e-9
and the default drive stays bit-identical).

### PHASE 4 — implement ✓ (Workflow: 2 WIs, pipeline)
- **WI-1 pure environment seam** (agent): `src/sim/environment.ts`
  (`environmentParams` μ+visuals, `cycleEnvironment` ring) + `test/environment.test.ts`
  (ENV1-6). Green. **Agent gotcha caught:** the module header originally said
  "no Date…" which tripped the G3 determinism grep-guard (greps `src/sim/*.ts` for
  the substring "Date") — reworded to "no wall-clock". Pure: no Three.js/DOM/clock/rng.
- **WI-2 world adapter** (agent): `scene.ts` (env applied each frame to
  sky/fog/lights/rain/rail-sheen; stations = platform+canopy+wall+board+lamps;
  lineside = OLE masts + fencing + distant buildings + mileposts, each one
  InstancedMesh built once), `cab.ts` (wiper gated by `wiperOn`), `main.ts`
  (`ep.mu`→resolveInputs replacing hardwired `ADHESION.wetNight`; `KeyE` demo
  cycle via pure `cycleEnvironment`), `package.json` 0.4.0→0.5.0. tsc+build clean.

### PHASE 4 — review ✓ (Workflow: 4 dims → 2-of-3 verify)
22 agents. **3 confirmed** (all minor/nit), 2 dropped by verification:
- main.ts:4 stale header ("`mu` stays hardwired ADHESION.wetNight") — now wrong;
  rewritten to say μ comes from `environmentParams(env)` (default pins to wetNight).
- scene.ts:26 stale JSDoc ("wet-night cab view") — reworded for the env system.
- ENV5's `μ ≥ 0.15` assertion vacuous (in-domain min is night×storm = 0.16 > floor)
  — added a test pinning the in-domain minimum = 0.16 and documenting MU_FLOOR as a
  defensive guard. All applied; check green at **121 tests**.

### PHASE 4 — lead screenshot verification + fix ✓
Headless Chrome (Playwright via system Chrome; bundled headless-shell absent — must
use the `channel:'chrome'` fallback). Confirmed: wet-night cab renders; **weather
system cycles** (day+clear bright + rain off ↔ wet-night) via `KeyE`; lineside
scenery, signals, motion, and cab instruments (the "red disc"/"yellow ring" were
never artifacts — they're the penalty lamp and the AWS CAUTION sunflower, large
only because they sit ~0.7 m from the eye); **zero page errors** (only favicon 404).
- **Two driver-script traps (not app bugs), journaled so they don't recur:**
  (1) `page.keyboard.press()` races the 16 ms frame — the keyup deletes the edge
  before any `frame()` reads it, so env never cycled / reverser stayed OFF; fixed
  with a frame-spanning `tap()` (down → wait → up). (2) The train spawns brake-full
  (anti-creep) and keydown ignores auto-repeat by design, so each brake detent needs
  a discrete tap. Also read chainage off the HUD (`CHAINAGE` label span → sibling)
  to gate screenshots by distance, not guessed iterations.
- **Lead finding → fix:** stations were built correctly (review-confirmed) but
  **too dark to read** — dark furniture against a near-black ground, and "day" only
  brightened the sky *background* not the surfaces. Lightened platform concrete/
  steel + name board emissive; raised the env ambient/moon ceilings (0.55→0.85,
  0.8→1.2 — ENV2 monotonicity preserved, ENV4 μ untouched); strengthened the platform
  PointLights (1.5/16 → 3.0/24) so the lit platform pools light as a night landmark.
  Re-shot: the **Riverside platform + canopy now read clearly** in the wet-night
  approach (z≈1396) and alongside (z≈1432, PSR 25 mph). check 121 green, build clean.

**PHASE 4 COMPLETE** → commit `129b5ad` (v0.5.0). **Aesthetic balance (wet-night
station legibility at speed, "day" warmth/whiteness) flagged for human review** per
the HLD residual-risk #1; the camera is locked forward so alongside-station framing
is inherently limited.

---

### PHASE 5 (Reach) — design ✓
Authored `wrk_docs/2026.06.15 - HLD - mdtrain2 Phase 5 Reach.md`, self-signed →
commit `f7c167f`. The load-bearing seam: a device-agnostic `InputActions` model so
keyboard, gamepad and touch all map to `ControlIntent` through ONE pure function,
with **provable keyboard parity** (REG-INPUT) making the refactor safe.

### PHASE 5 — implement ✓ (Workflow: 2 WIs, pipeline)
- **WI-1 pure seams** (agent): `src/input/intent.ts` (InputActions, NO_ACTIONS,
  mergeActions, intentFromActions, keyboardActions, gamepadActions), `src/ui/motion.ts`
  (motionParams), `src/render/quality.ts` (qualityFor) + `test/input.test.ts` +
  `test/reach.test.ts`. **REG-INPUT** pastes a verbatim copy of the legacy
  `intentFromKeys` and deep-equals it against `intentFromActions(keyboardActions(e))`
  over **70 edge subsets** (empty + 11 singletons + 55 pairs + full + KeyZ + KeyZ/KeyW
  + KeyE). 163 tests green (121 + 42 new).
- **WI-2 device adapters** (agent): `input/keyboard.ts` (edge-set ownership extracted
  from main, behaviour-identical), `input/gamepad.ts` (poll + edge-detect, no-op safe),
  `input/touch.ts` (fixed grid of ARIA-labelled `<button>`s, CSS injected once, shown
  only on coarse/`max-width:760px`), `scene.ts` widened to `createScene(parent,route,
  opts{rainCount,pixelRatioCap})` (rainCount 0 builds an empty buffer), `main.ts`
  composes `mergeActions(keyboard,gamepad,touch)→intentFromActions`, cycles env via
  `actions.cycleEnvironment`, applies the reduced-motion gate by building a FRESH
  EnvironmentParams (no mutation). v0.5.0→0.6.0. tsc + build clean.

### PHASE 5 — lead screenshot verification ✓ (all 3 SMOKE criteria)
Headless Chrome, two viewports:
- **(a) Desktop 1280×800:** touch overlay computed `display:none`; keyboard drives
  (POWER 3/4, REVERSER FWD, BRAKE RELEASE). Unchanged from Phase 4.
- **(b) Narrow 420×780 (isMobile):** touch overlay computed `display:grid` — a 4-col
  cab pad renders (PWR ±, BRK ±, REV F/N/R, DRA, ACK, red EMERGENCY span-2, ENV) below
  the portrait wet-night world.
- **(c) Touch-only drive:** firing `pointerdown` on the labelled buttons (REV F, BRK−×3,
  PWR+×3) took **POWER 0→3/4, REVERSER→FWD, BRAKE→RELEASE with no keyboard** — touch
  fully drives. Zero page errors (favicon 404 only). The touch edge model needs no
  frame-spanning trick (pending persists until `actions()` drains it), unlike the
  keyboard race noted for Phase 4.

### PHASE 5 — review ✓ (Workflow: 4 dims → 2-of-3 verify)
7 agents. **0 code-change findings.** One confirmed minor (2/3), purely documentary:
the HLD §3 impact map listed `index.html | CHG` for touch styles, but `index.html`
is byte-for-byte unchanged — `touch.ts` injects the responsive overlay CSS (incl.
the `@media (pointer: coarse),(max-width:760px)` query) via a one-time `<style>`.
The deliverable exists; only its *location* differs from the plan, and co-locating
the CSS with its adapter is cleaner. **Decision: accept the deviation, document it
(here + commit) — no code change.** (`lang="en"` + device-width viewport already
satisfy the a11y sanity pass.) The correctness/fidelity/determinism dimensions came
back clean and independently validated the tests (REG-INPUT parity, IN/ACC/QUAL);
one dimension agent (tests) died without returning structured output (API), but its
scope was fully covered by the other three — not re-run (proportional).

**PHASE 5 COMPLETE** → pending commit (v0.6.0). 163 tests green, tsc + build clean,
src/sim untouched. **Ergonomic feel of the touch pad on a real phone, and the
gamepad button map, are flagged for human review** (gamepad can't be exercised
headlessly; covered by the pure `gamepadActions` oracle + a no-op-safe shell).

---

## POST-SHIP TWEAKS (after almanac wiring, on Arthur's feedback)
- **"Too dark — can't see anything."** The wet-night signature default was the
  problem I'd flagged. Changed `DEFAULT_ENVIRONMENT` to **day+rain (rainy day)** —
  bright, fully legible — and reordered `PRESET_RING` to rainy day → rainy sunset →
  wet-night → clear day (all still on `E`). ENV4 re-pinned: default μ = 0.25; a
  second test keeps the wet-night preset pinned at μ = 0.20 == `ADHESION.wetNight`.
  Arthur picked rainy day from a day/sunset/night screenshot comparison.
- **No on-screen key help.** Added `src/ui/help.ts` — a self-contained top-right
  CONTROLS card (owns its own `H` toggle + close button; hidden on coarse-pointer
  where the touch buttons self-label). One import + one call in `main.ts`; no sim/
  input-core change, like `hud.ts`/`touch.ts`. v0.6.0 → 0.7.0. 164 tests green.

## CAMPAIGN COMPLETE (Phases 4 + 5)
Both phases designed (self-signed HLDs), implemented via Workflow pipelines, reviewed
with adversarial verification, screenshot-verified, and committed. mdtrain2 at v0.6.0.
Plan's Phases 0-5 are now all delivered. NOT pushed (no explicit push instruction;
standing rule). Outstanding for human eyes/hands: wet-night station legibility & "day"
warmth (P4 aesthetics); touch-pad ergonomics & gamepad mapping (P5); and the
long-standing call on wiring mdtrain2 into the almanac as The Cab's successor.
