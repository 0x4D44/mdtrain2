# 2026.06.14 — JRN — mdtrain2 Phase 3 (Presentation): the wet-night cab + GTO audio

Lead: Claude (full autonomous authority incl. self-sign-off — Arthur granted "full autonomy, both").

## Kickoff decisions (team lead)
- **Proportionality (SCALE):** Phase 3 is PRESENTATION — 3D cab interior, wet-night atmosphere, GTO inverter audio. It is **aesthetic, low-design-risk, and mostly NOT unit-testable** (Three.js geometry + WebAudio graph). A heavy ultracode design cycle (3 drafts + judges + 3 review rounds + Codex) is disproportionate for "draw a cab, add rain, synthesise a whine." **Decision: author a focused HLD directly (team lead), isolate the few testable seams, self-sign-off, and put the effort into implementation + build/screenshot verification.** (Contrast: Phases 1-2 had load-bearing testable invariants — a shared penalty latch, a signalling state machine — that warranted the heavy review; Phase 3 does not.)
- **Verification honesty:** my autonomous verification of Phase 3 is limited to: `npm run check` green (sim tests + pure audio-mapping oracles), `npm run build` clean, a headless screenshot showing the cab + wet-night scene with zero console errors, and the audio graph instantiating. **Aesthetic quality (does the cab look good? does the GTO whine sound right?) needs human eyes/ears** — flagged for the morning report.
- **Testable seam carved out:** the sim-state→audio-param mapping is a PURE function (`audioParams(speed,notch,brake,...)`) with real oracles (whine frequency rises with speed; traction gain follows notch; silent at rest+no power). The Three.js/WebAudio sit behind thin impure adapters.

## Phase log
### Design — author HLD + self-sign-off ✓
Authored `wrk_docs/2026.06.14 - HLD - mdtrain2 Phase 3 Presentation.md` directly and **self-signed-off** (Status: SIGNED-OFF). Three pillars: (1) 3D cab interior (`cab.ts`) bound to control/safety/speed via a per-frame `CabView`; (2) wet-night world (`scene.ts`: night lighting, rain particles, 3D signal heads glowing with live `aspectAt` aspects, wet-rail sheen); (3) GTO audio (`audio/params.ts` PURE+tested mapping + `audio/engine.ts` WebAudio graph). Binds entirely to existing sim state — **no sim-core/gameplay change**. The render seam widens `scene.render(chainage)` → `render(view: RenderView)`. Acceptance: AUD1-5 (pure audioParams oracles) + 105 sim tests + build + headless screenshot (cab over rainy scene w/ glowing signal, zero errors, AudioContext instantiates). Aesthetic quality explicitly flagged as needing human review.

### IMPLEMENT — PLAN ✓ (lead)
- **WI-1 — pure audio mapping.** `src/audio/params.ts` (`audioParams(speed,notch,brake)`) + `test/audio.test.ts` (AUD1-5). DoD: check green.
- **WI-2 — presentation layer (impure, screenshot-verified).** `scene.ts` wet-night world + `cab.ts` + `audio/engine.ts` + `main.ts` wiring + v0.4.0. DoD: build clean + headless smoke (screenshot + zero errors + AudioContext) + 105 sim tests still green.

### IMPLEMENT — WI-1 ✓ → commit `d053f35`
`src/audio/params.ts` (pure `audioParams`) + `test/audio.test.ts` (AUD1-5). whineHz monotonic in |speed| (60→~370 Hz, sqrt spool front-load); gains clamped/finite. 111 tests green.

### IMPLEMENT — WI-2 ✓ → commit `078789f`
`scene.ts` (wet-night world: night lighting, 2400-particle rain, 3D signal heads glowing per live `aspectAt`, wet-rail sheen; widened `render(view)`), `cab.ts` (driver's-eye cab as camera children: windscreen+wiper, desk, power/brake levers, reverser, speedo, AWS sunflower, DRA/DSD/PENALTY lamps + cab light), `audio/engine.ts` (WebAudio whine/roll/hiss, gesture-start, no-op-safe), `main.ts` wiring, v0.4.0. **src/sim untouched.**
- **Bug found + fixed via screenshot (lead):** the cab was placed at local **+Z** with a comment claiming the camera looks down +Z — but Three.js cameras look down local **−Z**, so the whole cab was *behind* the camera (invisible in the first screenshot). Negated the furniture Z (translation, not a mirror — keeps L/R) + added a dim cab light. Re-screenshot: cab renders (windscreen, green power lever, red brake lever, wiper) over the rainy night scene, zero errors.

### IMPLEMENT — REVIEW ✓ (one thorough reviewer — proportional to an aesthetic, screenshot-verified phase)
Verdict: **sound and faithful to the HLD; 0 critical/major.** Verified: audio drives freq/gains each frame + single-start + no-op-safe; cab binds correct values; no per-frame allocation (rain/signal loops); src/sim genuinely untouched (empty diff); the −Z cab fix correct. Three minors (speedo sweep direction = human look-check; penalty audio-vs-lever divergence = intentional; MPS_TO_MPH duplication = harmless) — no code change needed.

## PHASE 3 COMPLETE ✓
Design self-signed + implemented + reviewed. Commits `57973f4` (HLD), `d053f35` (WI-1), `078789f` (WI-2). Final: `npm run check` green (5 files, **111 tests**), build clean, src/sim determinism clean, v0.4.0, headless screenshot shows the wet-night cab. **Aesthetic quality (cab look, GTO whine sound) flagged for human review.** NOT pushed (no explicit push instruction; standing rule).
