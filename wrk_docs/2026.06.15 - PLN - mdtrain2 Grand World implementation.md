# PLN — mdtrain2 "Grand World" implementation

**Date:** 2026-06-15 · **Lead:** Claude (ultracode) · derives from the SIGNED-OFF
`wrk_docs/2026.06.15 - HLD - mdtrain2 Grand World.md`. VALIDATE passed (HEAD `14cddef`,
v0.10.0, 164 tests green; no PARK).

This plan splits the HLD into work items with **clean file partitions**, ordered so each
lands as a complete, tsc-clean, test-green unit. Oracle IDs (O1…) are from HLD §4 — they
are the definition of done.

## Partition principle
- **Pure layer** (`src/sim/*`, `src/render/quality.ts`): one owner per module **plus its
  test file**; vitest + `tsc --noEmit` gate each. Dependency-ordered (the spatial core is
  a chain). Banned-token-free.
- **Impure layer** (`src/render/{scene,scenery,cab,terrain-mesh,textures}.ts`, `main.ts`,
  `scripts/check-dist.mjs`, `package.json`): no unit tests (screenshot-gated); built once
  the pure core is stable; validated by `tsc` + `vite build` + `check-dist.mjs` + S1–S6.
- **No two agents edit the same file concurrently.** Test-file ownership is exclusive
  (below). `test/signalling.test.ts` is touched **only** by the guards WI (G3 block only).

## Phase 3a — PURE CORE (dependency-ordered; each commits green)

| WI | Owns (src) | Owns (test) | Depends on | Oracles (DoD) |
|---|---|---|---|---|
| **P1 route** | `src/sim/route.ts` | `test/route.test.ts` (NEW) | — | `minCurveRadius` (incl. all-straight→+∞); O19 (KINGSGATE radii ≥ 400 where curved); O19b (viaduct/tunnel bands on κ=0 segs); sign convention (`valleyDepth>0`); chainage/station sanity |
| **P2 centerline** | `src/sim/centerline.ts` (NEW) | `test/centerline.test.ts` (NEW) | P1 | O1–O3 (height ∫grade), O4 (tangent G1), O5/O5b (arc closed-form + 2-arc pose), O5c (`placeOnCentreline` perpendicular + **left/right sign**), O6 (cant) |
| **P3 terrain** | `src/sim/terrain.ts` (NEW) | `test/terrain.test.ts` (NEW) | P2 | O7 (anchorY), O8 (+O8-edge C0 across band edge, +non-vacuity m=1 exists), O8b (track on bed), O8c (pier feet), O9 (macro predicates + bounded noise), O20 (clampEye), O-DOMAIN, `bandMembership` monotone/∈[0,1]/seed-independent |
| **P4 camera** | `src/sim/camera.ts` (NEW) | `test/camera.test.ts` (NEW) | P2,P3 | O5d (pure: `+π` under YXZ ⇒ looks +Z), O16 (cab attitude pinned + non-tautological + scale=0⇒{0,0}) |
| **P5 environment** | `src/sim/environment.ts` | `test/environment.test.ts` | — (parallel) | O10–O12 (exposure ordered, `\|sunDir\|=1`, pure/finite); extend ENV5 finite guard for new scalar fields |
| **P6 quality** | `src/render/quality.ts` | `test/reach.test.ts` | — (parallel) | O13 (tier order), O14 (`lodForDistance` exact 120/350, tier-independent), O15 (domain sweep), O18 (`ribbonVertexCount` HIGH 80k/LOW 25k); extend desktop `toEqual` |
| **P7 guards** | — | `test/world.test.ts` (NEW) + G3 block in `test/signalling.test.ts` | P1–P6 | O17 (G3: array-glob `src/sim/*.ts`+`src/render/quality.ts`, comment-stripped, `\b`-anchored, positive-control, non-vacuity; **+ NEW import-boundary layering** parse), O-RIBBON (KINGSGATE; WESTFORD exempt) |

**Order:** P1 → P2 → P3 → P4 → (P5 ∥ P6) → P7.
**Each WI agent:** writes the oracle tests first (from the HLD's exact pins), implements
until `npx vitest run <own test>` is green, `npx tsc --noEmit` clean, and the **full suite
still green (≥164, no REG deletions/weakenings)**; edits only its owned files; returns a
structured report (files, oracles, pass/tsc, deviations).
**Commit** after lead review: likely 2 atomic commits — (a) spatial core P1–P4, (b) seams
+ guards P5–P7. `package.json` version bump deferred to the impure-layer commit.

## Phase 3b — IMPURE RENDER LAYER (build + screenshot-gated; after 3a committed)

| WI | Owns | Depends on | Gate |
|---|---|---|---|
| **R1 textures** | `src/render/textures.ts` (NEW) | — | tsc; consumed by scene |
| **R2 terrain-mesh** | `src/render/terrain-mesh.ts` (NEW) | P2,P3,P6 | tsc; sectioned/seam-welded ground+track ribbon, bore cutout, far band |
| **R3 scene** | `src/render/scene.ts` (CHG, major) | P2–P6,R1,R2 | tsc; eyePose+eased attitude+`+π`; lineside/set-pieces via `placeOnCentreline`+facing; ACES once; async bloom; tiny env map; eye-tracking shadow; viaduct/tunnel; **delete flat ground**; rain anchored to eye |
| **R4 scenery** | `src/render/scenery.ts` (CHG) | P2,P3,P6 | tsc; delete `addBank`+cone-hills; props via `placeOnCentreline`+`anchorY`; LOD |
| **R5 cab** | `src/render/cab.ts` (CHG, tiny) | — | tsc; mount takes eased pitch+roll |
| **R6 wiring** | `src/main.ts`, `scripts/check-dist.mjs` (NEW), `package.json` | all | tsc; `vite build`; `node scripts/check-dist.mjs`; **0.10.0→0.11.0** |

R3 is the integrator and imports R1/R2/R4/R5 + the pure core — it cannot be edited
concurrently with files it pulls in. Sequence: **R1,R2 (leaf) → R4,R5 → R3 → R6.** Because
this layer is tightly coupled and untested-by-unit, it is implemented as a coordinated
sequence (not wide fan-out), then validated by tsc + build + `check-dist.mjs` + screenshots.

## Acceptance (HLD §4) — overall DoD
- All pure oracles O1–O22 (+O5b/c/d, O8b/c, O-DOMAIN, O-RIBBON) green.
- **REG: the existing 164 tests pass unchanged** (no deletions/weakened assertions;
  `WESTFORD_EASTBANK` retained as fixture). New oracles are additive ⇒ total > 164.
- `tsc --noEmit` clean; `vite build` clean; `node scripts/check-dist.mjs` passes
  (incl. no `UnrealBloomPass`/`EffectComposer` in the main chunk).
- Screenshot gates S1–S6 (cutting+embankment; viaduct over open valley + tunnel bore;
  curve-following + banked cab + **camera faces forward**; ACES/shadow/sky/bloom/sheen;
  mobile gate incl. bloom chunk not requested; desktop drives, driver-left, no cab clip).
- ENV4 μ=0.20 route-independent (physics bit-identical).

## Risks carried from the HLD
R1/R5 ACES re-tune (top schedule risk, needs human eyes), R2 perf on 14 km, R3 cab comfort,
R7 tunnel readability across day & night×rain, R11 two-surface masonry alignment — all
screenshot-gated, flagged to the morning report.
