# HLD — mdtrain2 Phase 1: Driving (Control + HUD Layer)

**Status:** under review (V5) · **Author:** design subagent · **Date:** 2026-06-14
**Target:** `C:\language\mdtrain2` (Vite + TS + Three.js + Vitest; Phase 0 complete)
**Acceptance gate:** `npm run check` (`tsc --noEmit && vitest run`) green, zero TS warnings.

---

## 1. Scope & non-goals

### In scope (control + HUD layer only)
- A **pure controls/safety reducer** (`src/sim/controls.ts`) holding `ControlState` + `SafetyState`, exposing the testable functions `reduceControls`, `tickSafety`, `resolveInputs`, and `buildHudView` that turn driver intent + timed safety into the existing `SimInputs` for `step()`.
- **Prototypical stepped controls:** notched POWER (OFF + 4 notches), stepped BRAKE (RELEASE + 3 service applications — STEP 1, STEP 2, FULL SERVICE — + EMERGENCY past an end gate; index 3 **is** FULL SERVICE), REVERSER (FORWARD/OFF/REVERSE).
- **Interlocks:** no power against an applied brake; reverser changes only with power off and at a stand; reverser OFF inhibits power.
- **Safety now:** the DSD/vigilance device (timer flavour) feeds **one shared penalty latch + one acknowledge**; the DRA is a **separate latching power-inhibit** (no brake, not a penalty reason).
- **One surgical pure-physics change:** a real, distinct EMERGENCY deceleration (`brakeEmergencyDecel=1.3`) selected in `brakeForce`, with its own analytic oracle — **not** faked as `brake=1`.
- A **keymap→intent input layer** kept **inline in `main.ts`** (decision-free ~20-line lookup; no separate module).
- A **structured DOM HUD** split into a pure `buildHudView` (tested) + a thin `createHud` DOM writer (untested).
- A **typed AWS/TPWS no-op stub** at `src/sim/safety/aws.ts` defining the Phase-2 seam and passing through unchanged.
- A thin, deliberately-untested `main.ts` wiring keyboard → controls → safety → `resolveInputs` → `step` → `render` → HUD.

### Non-goals (explicit — do not build)
- 3D cab interior, lighting, GTO audio (Phase 3); `scene.ts`, `index.html` 3D world unchanged.
- AWS/TPWS *behaviour* — magnets, deadlines, sunflower, SPAD (Phase 2). Phase 1 ships only the seam + no-op stub.
- Gamepad/touch input (Phase 5); keyboard only.
- Route signal/AWS-magnet positions, station-stop scoring (no data; later phases).
- Pneumatic pipe-propagation, per-axle brake modelling, continuous (analog) levers.
- Widening `SimInputs.dir` to include neutral `0` (breaking — `dir` stays `1|-1`; neutral lives only in the control layer).
- New state on `SimState`; latches/timers inside `step()`/`physics.ts`.
- Exposing `mu` to the driver — stays hardwired `ADHESION.wetNight`. No env vars, no feature flags, no CI changes, no new runtime deps.

---

## 2. The design

### 2.0 Risk-first overview — the five failure modes, designed away

This table leads because the rest of the design exists to defeat these. Each row maps a failure mode to the single design move that prevents it; the oracle that pins it is in §4.

| # | Failure mode | How it bites | Design move that prevents it | Oracle |
|---|---|---|---|---|
| **R1** | **Shared latch fights itself** (DSD now, AWS/TPWS later) | Two systems each "own" a latch; one clears it while the other still demands brake; or ack clears DSD but AWS re-latches next tick → un-releasable or prematurely-released brake | **One latch = `Set<PenaltyReason>`, active iff non-empty.** Each subsystem only adds/removes *its own* reason. Documented per-reason clearability predicate (§2.6). Ack is edge- and stand-gated. | O9–O12 |
| **R2** | **Interlock composition order** | "No power vs brake", DRA, penalty, reverser-OFF all mutate `notch`/`brake`; wrong order → power leaks through a penalty, or a released DRA still blocks | **One pure `resolveInputs`** with a fixed pipeline: power-inhibitors compose by OR, brake-demands by `max`. Order-independent by construction; each inhibitor pinned in isolation. | O3–O5, O12, O15a–e |
| **R3** | **Emergency change breaks existing oracles** | The braking-distance oracle (`physics.test.ts:109`) drives `brake:1` and asserts `brakeServiceDecel`. Reinterpreting `brake=1` as emergency breaks it. | Keep `brake∈[0,1]` scaling **service** decel. Add a **separate `emergency` channel** through `AccelInputs`/`SimInputs`→`brakeForce`, default `false`. | O1–O2, regression |
| **R4** | **Wall-clock / determinism leak** | A `performance.now()`/`setTimeout` in DSD → non-reproducible, frame-rate-dependent vigilance | **All timed safety advances off the explicit `dt`** passed to `tickSafety`. No `Date`/`performance.now`/`setTimeout`/`Math.random` anywhere under `src/sim` or `src/input`. Verifiable by grep + a frame-rate-independence oracle. | O8 |
| **R5** | **`brakeActual=1` start mismatch** | `createInitialState` seeds `brakeActual=1`. If the brake controller starts at RELEASE, HUD shows a released lever against a fully-applied brake and frame 1 could roll. | **`createInitialControls()` seeds `brakeStep = FULL SERVICE`** so lever, demand, and `brakeActual` all agree at spawn. | O3 (no-creep) |

**On the stale speed read (R2/R5):** the interlocks (reverser-at-stand, DRA-at-stand, motion-gated vigilance, latch release) read the **pre-step**, one-frame-stale `state.speed` — the value from before this frame's `step()` runs. **What the through-zero snap guarantees, exactly:** when a notch is suppressed (an inhibitor fires) *and* speed is at or crossing zero, `step()`'s through-zero snap zeroes the speed, so a one-frame-stale "at a stand" reading can never let **power-induced** motion leak through — that is the no-creep guarantee, and nothing more. It does **not** claim that a brake-released, power-inhibited train will stay put on a gradient: such a train can roll under gravity, which is correct physics governed by the **brake**, not by the power interlock. The snap suppresses power-induced creep at/through zero; it is not a parking brake.

### 2.1 Module map

```
src/sim/controls.ts     NEW  pure: ControlState, SafetyState, reduceControls, tickSafety,
                                    resolveInputs, buildHudView, interlocks, penalty latch, constants
src/sim/safety/aws.ts   NEW  pure: AwsOutput + tickAws() no-op fold point (Phase-2 home for AWS/TPWS)
src/sim/physics.ts      CHG  brakeForce gains an `emergency` arg; AccelInputs gains `emergency`
src/sim/simulation.ts   CHG  SimInputs gains `emergency: boolean`; step threads it to acceleration()
src/ui/hud.ts           NEW  impure-thin: createHud(parent) → { update(view) }  (buildHudView lives in controls.ts)
src/main.ts             CHG  thin shell: inline keymap, rAF, edge-set, wiring — untested glue
index.html              —    unchanged (HUD builds its own DOM overlay)
```

**Why one pure brain (`controls.ts`) + thin shell.** The whole control/safety/HUD-derivation logic is a single pure module under `src/sim/`, unit-tested with the same discipline as `physics.ts`. The keymap is a ~20-line decision-free lookup, so per the brief's "keep the split minimal / don't predict the future" it stays **inline in `main.ts`** rather than as a speculative `src/input/keymap.ts` — the *meaning* of keys is trivial and the only untested part (edge-set construction) is untested either way. `src/ui/` gains exactly one file (`hud.ts`, the DOM writer); the load-bearing HUD projection (`buildHudView`) lives in the tested `controls.ts`. The AWS stub goes under `src/sim/safety/` to signal the Phase-2 home for the AWS/TPWS state machine without over-building it.

Dependency direction: `main.ts` → {`sim/*`, `ui/hud`}. The `sim/*` modules never import from `ui` or `render`. `controls.ts` imports `safety/aws.ts`. Pure modules import no DOM, no `three`.

### 2.2 Core types

```ts
// src/sim/controls.ts
export type Reverser = "FWD" | "OFF" | "REV";
export type PenaltyReason = "DSD" | "AWS" | "TPWS"; // AWS/TPWS reserved for Phase 2

export const POWER_NOTCHES = 4;                          // OFF + 4 detents
export const BRAKE_SERVICE_STEPS = 3;                    // service steps 1..3
export const BRAKE_FULL = BRAKE_SERVICE_STEPS;           // index 3 = FULL SERVICE
export const BRAKE_EMERGENCY = BRAKE_SERVICE_STEPS + 1;  // index 4 = EMERGENCY (past the end gate)
export const STAND_EPS = 0.3;   // m/s — "at a stand" (reverser, DRA, latch release)
export const DSD_PERIOD = 60;   // s vigilance interval
export const DSD_WARN_WINDOW = 7; // s of warning before penalty

export interface ControlState {
  powerNotch: number;   // 0..POWER_NOTCHES   (integer index; 0 = OFF)
  brakeStep: number;    // 0..BRAKE_EMERGENCY (0=RELEASE, 1=STEP 1, 2=STEP 2, 3=FULL SERVICE, 4=EMERGENCY)
  reverser: Reverser;
  lastDir: 1 | -1;      // remembered heading; reverser OFF keeps this, feeds SimInputs.dir; never 0
  dra: boolean;         // Driver's Reminder Appliance: set ⇒ power inhibited
}

export interface SafetyState {
  vigilanceTimer: number;           // s remaining before warning; counts DOWN off dt
  dsdWarning: boolean;              // warning window active (amber lamp)
  penaltyReasons: ReadonlySet<PenaltyReason>; // the SHARED latch: active iff non-empty
}

export function createInitialControls(): ControlState {
  return { powerNotch: 0, brakeStep: BRAKE_FULL, reverser: "OFF", lastDir: 1, dra: false };
  //                                  ^ FULL SERVICE at spawn ⇒ matches brakeActual=1 (R5)
}
export function createInitialSafety(): SafetyState {
  return { vigilanceTimer: DSD_PERIOD, dsdWarning: false, penaltyReasons: new Set() };
}
```

**Integer indices are the source of truth** for power/brake (not floats): notch fraction `powerNotch / POWER_NOTCHES` → exactly {0, .25, .5, .75, 1}; service brake fraction is defined explicitly as `brakeFraction(brakeStep) = clamp01(brakeStep / BRAKE_SERVICE_STEPS)` → {0, ⅓, ⅔, 1} for steps 0..3, so **FULL (index 3) → 1** and **EMERGENCY (index 4) → clamp01(4/3) = 1**. That is: **both FULL and EMERGENCY map to brake demand `1`.** The EMERGENCY *physics* difference does not come from the brake fraction at all — it comes solely from the separate `emergency` boolean (§2.5 step 5 / §2.7), which selects `brakeEmergencyDecel`. This clamped definition is load-bearing for oracles O5 and O12. Exact fractions make `notch === 0.5` assertions trivial and avoid float drift. Pure helpers `notchFraction`, `brakeFraction`, `isEmergency`, `brakeApplied`, `penaltyActive(s) = s.penaltyReasons.size > 0` are exported for HUD + tests.

### 2.3 The intent + the inline keymap (in `main.ts`)

`ControlIntent` is a flat record of edges for one frame, applied by the pure `reduceControls`/`tickSafety`:

```ts
// declared in controls.ts (consumed by the pure reducers); produced inline in main.ts
export interface ControlIntent {
  powerUp: boolean; powerDown: boolean;                 // edge: one detent per press
  brakeUp: boolean; brakeDown: boolean;                 // edge
  emergency: boolean;                                   // edge: slam to EMERGENCY
  reverserFwd: boolean; reverserOff: boolean; reverserRev: boolean; // edge
  toggleDra: boolean;                                   // edge
  acknowledge: boolean;                                 // edge: DSD/penalty ack
  vigilancePing: boolean;                               // any control activity this frame ⇒ resets DSD
}
```

`main.ts` maintains a per-frame **edge set** of just-pressed key codes (`keydown` with `if (e.repeat) return` so a hold never auto-repeats a detent; `keyup`/`blur` housekeeping), then builds the intent from a ~20-line literal lookup table (e.g. `KeyW`→powerUp, `KeyS`→powerDown, `KeyD`→brakeUp, `KeyA`→brakeDown, `Backquote`→emergency, `KeyF`/`KeyN`/`KeyR`→reverser FWD/OFF/REV, `KeyL`→toggleDra, `KeyQ`→acknowledge), with `vigilancePing = edgeSet.size > 0`. The table is decision-free data; the only untested logic is the edge-set construction, which is the same handful of lines whether or not the table lives in its own file — so it stays inline (R: simplicity, "don't predict the future").

**Simultaneous opposing/conflicting edges in one frame** are resolved deterministically before reduction (and pinned by O20): `emergency` overrides all other brake edges (it wins outright); an opposing `brakeUp`+`brakeDown` in the same frame cancel (no brake change); an opposing `powerUp`+`powerDown` cancel (no power change); and if more than one reverser edge (`reverserFwd`/`reverserOff`/`reverserRev`) is present in the same frame, all of them are ignored (no reverser change). This makes a frame carrying contradictory edges well-defined rather than order-dependent.

**Vigilance is reset by ANY control edge** (`vigilancePing = edgeSet.size > 0`), the acknowledge key included. Because the acknowledge key is itself a control edge, it sets `vigilancePing` and so resets the vigilance timer (via `tickSafety` step 1, §2.6) **even on a tick where it does not release the latch** (e.g. moving, so no release) — this coupling is intended: any driver input is vigilance activity. Because `keydown` uses `if (e.repeat) return`, simply *holding* a steady power notch produces no further edges — so a driver who makes no fresh input for `DSD_PERIOD` while moving will (correctly, by design) trip the vigilance penalty. This is the intended deadman behaviour, not a bug: vigilance demands periodic positive driver action, not a held lever.

### 2.4 Control reduction (pure, `controls.ts`)

```ts
export function reduceControls(c: ControlState, intent: ControlIntent, state: SimState): ControlState;
```

Applies, in order, returning a new state (mutates nothing). The frame's edges are first conflict-resolved per §2.3 (emergency overrides brake edges; opposing power/brake edges cancel; multiple reverser edges are ignored):

1. **Reverser** edges: a change to FWD/OFF/REV is accepted **only if `powerNotch === 0` AND `|state.speed| ≤ STAND_EPS`** (power must be off *and* the train at a stand); otherwise ignored. FWD sets `lastDir=1`; REV sets `lastDir=-1`; OFF keeps `lastDir`.
2. **Power** edges: `powerNotch = clamp(powerNotch ± 1, 0, POWER_NOTCHES)`.
3. **Brake** edges: `brakeUp` steps `0→1→2→3(FULL)→4(EMERGENCY)`; `brakeDown` decrements one step clamped to `[0, BRAKE_EMERGENCY]`. **EMERGENCY is reachable only by stepping past FULL (an end gate)** — a single `brakeUp` from FULL reaches EMERGENCY; it is *not* reachable by single-keypress overshoot from a partial step. The `emergency` edge slams `brakeStep = BRAKE_EMERGENCY` directly. **EMERGENCY latches:** a `brakeDown` from EMERGENCY(4)→FULL(3) is **rejected unless the train is at a stand** (`|state.speed| ≤ STAND_EPS`); a `brakeDown` from any *service* step (3→2→1→0) decrements freely. So once EMERGENCY is applied it cannot be stepped down while moving — it releases to FULL only at a stand (D18, O19).
4. **DRA** toggle: accepted **only at a stand** (`|state.speed| ≤ STAND_EPS`).

`reduceControls` owns the reverser-at-stand and DRA-at-stand guards, the EMERGENCY-release-at-stand latch, and the stepped clamps. It does **not** apply the no-power-against-brake interlock — that is a demand-time gate (the lever can sit in notch 2 while effort is suppressed) and belongs in `resolveInputs`.

**Residual-risk note (reverser at a crawl):** `STAND_EPS` (0.3 m/s) is the "at a stand" threshold; because `step()`'s through-zero snap only zeroes speed when `notch === 0`, a braked train can linger just below `STAND_EPS`, so a reverser change is *technically* permitted at a crawl (≤ 0.3 m/s ≈ 0.67 mph). This is benign: the reverser-change interlock already requires `powerNotch === 0` (D16), which makes the through-zero snap active, so in practice the change happens at a true stand. No extra constant is introduced.

### 2.5 `resolveInputs` — the interlock pipeline (the PRIMARY SEAM)

The single pure function turning control + safety state into `SimInputs`. Fixed, documented composition order; power-inhibitors OR-compose, brake-demands `max`-compose:

```ts
export function resolveInputs(c: ControlState, s: SafetyState, mu: number): SimInputs;
```

```
1. dir          = c.lastDir                                   // always 1|-1; OFF is expressed as zero notch
2. penaltyOn    = penaltyActive(s)
3. powerInhibit = brakeApplied(c.brakeStep)                   // no power vs applied brake
               || c.reverser === "OFF"                        // reverser OFF inhibits power
               || c.dra                                       // DRA inhibits power
               || penaltyOn                                   // penalty inhibits power
4. notch        = powerInhibit ? 0 : notchFraction(c.powerNotch)
5. emergency    = isEmergency(c.brakeStep)                    // ONLY the driver's EMERGENCY lever; penalty ≠ emergency
6. driverBrake  = brakeFraction(c.brakeStep)                  // clamp01(step/3): FULL→1, EMERGENCY→clamp01(4/3)=1
   penaltyBrake = penaltyOn ? 1 : 0                           // penalty = FULL SERVICE demand
   brake        = Math.max(driverBrake, penaltyBrake)
7. return { notch, brake, dir, emergency, mu }
```

Guarantees pinned by oracles: any applied brake OR penalty OR DRA OR reverser-OFF ⇒ `notch === 0`; the driver can never out-rank a penalty (penalty forces full-service brake + zero power regardless of lever positions); **penalty brake is full *service*, not emergency** (`emergency` is reserved for the driver's deliberate EMERGENCY lever — matches the brief's "full-service, latched" penalty).

The safety layer expresses its override purely as `brake`/`notch`/`emergency` floats — exactly the `step()` contract. `step` keeps owning `brakeActual` (the lag); the safety layer never touches it. The *only* new `SimInputs` field is `emergency`, a genuine physics input, not a safety flag.

### 2.6 DSD/vigilance, DRA, and the shared penalty latch (pure, `controls.ts`)

```ts
export function tickSafety(
  s: SafetyState, intent: ControlIntent, state: SimState, dt: number, aws: AwsOutput,
): SafetyState;
```

Order inside `tickSafety` (returns a new state; advances only off `dt` — R4):

1. **Vigilance reset:** if `intent.vigilancePing` → `vigilanceTimer = DSD_PERIOD`, `dsdWarning = false`. Because the acknowledge key is itself a control edge it raises `vigilancePing`, so an ack always resets the vigilance timer here even on a tick where it does not release the latch (intended — any driver input is vigilance activity, §2.3).
2. **Countdown (motion-gated):** else if the train is moving (`|state.speed| > STAND_EPS`), `vigilanceTimer -= dt` and `dsdWarning = vigilanceTimer ≤ DSD_WARN_WINDOW`; when `vigilanceTimer ≤ 0`, **add `"DSD"`** to `penaltyReasons`. **At a stand (`|state.speed| ≤ STAND_EPS`) the timer holds (does not decrement)**, so a correctly parked train is never penalty-braked.
3. **AWS/TPWS merge:** fold in `aws.reasons` (from the stub, §2.8) at the *same* point DSD adds its reason — Phase 1 contributes none.
4. **Latch release (the one ack path):** apply the per-reason clearability predicate below.

**The CLEAR → WARNING → PENALTY state machine and the release edge:**

```
        ping (timer > WARN)                  ack edge AND |speed| ≤ STAND_EPS
        ┌────────────────────────┐                      (releases reason)
        ▼                        │                           ▲
    [CLEAR] ──timer≤WARN──▶ [WARNING] ──timer≤0──▶ [PENALTY: reasons ∋ DSD]
        ▲                        │                           │
        └──── ping (reset) ──────┘                           │
                                                             │
   release a reason  ⟺  ack edge  AND  |speed| ≤ STAND_EPS    │
                                                              ┘
   latch clears  ⟺  penaltyReasons becomes empty
```

**Documented contract (lives as a comment in `controls.ts`, so the Phase-2 author wiring AWS/TPWS cannot break releasability):**

> A reason is removed from `penaltyReasons` on an `acknowledge` edge **AND** `|state.speed| ≤ STAND_EPS`. The overall latch is "released" only when the set becomes empty — that empty-set rule is the mandated invariant. In Phase 1 the only reason is `"DSD"`: its demand is satisfied once the driver acks at a stand (and, the train being stopped, the motion-gated timer holds rather than re-counting), so the ack-at-stand edge clears it.
>
> *Forward-looking note (Phase 2):* a reason whose source still demands brake **after** ack (e.g. an AWS magnet just passed) should additionally fail a "source no longer demands it" predicate and stay in the set; such reasons slot in **without touching the release machine** — one ack at a stand clears all currently-clearable reasons while a still-asserting subsystem keeps its reason and the brake stays applied. Phase 1 does not implement this clause (no persisting reason exists yet).

**Intended deadman behaviour (not a bug).** The vigilance timer is reset by *any* control edge (`vigilancePing = edgeSet.size > 0`, §2.3), the acknowledge key included. Because `keydown` uses `if (e.repeat) return`, holding a steady power notch produces no further edges, so a driver who makes no fresh input for `DSD_PERIOD` while moving will correctly trip the vigilance penalty. This is by design — vigilance demands periodic positive action, not a held lever — and is pinned from both sides: O9 (no input while moving ⇒ penalty) and O18 (periodic pings while moving ⇒ never penalised).

**DRA** is **control state**, not a penalty reason: it only inhibits power (§2.5), applies no brake, and is cancelled manually at a stand. It must not feed the latch (a reminder appliance is not a protection event), so it lives in `ControlState` as a boolean + lamp.

### 2.7 The physics emergency-decel change (surgical, R3)

`brakeForce` is called from exactly one site (`acceleration`) and has no direct unit test today, so the blast radius is one parameter. Add an `emergency` flag selecting the decel target:

```ts
// physics.ts
export function brakeForce(
  spec: TrainSpec, brakeActual: number, mu: number, emergency = false,
): number {
  const decel = emergency ? spec.brakeEmergencyDecel : spec.brakeServiceDecel;
  const demanded = clamp01(brakeActual) * decel * effectiveMass(spec);
  const adhesionLimit = mu * spec.mass * G; // braked on all axles
  return Math.max(0, Math.min(demanded, adhesionLimit));
}
```

`AccelInputs` gains a required `emergency: boolean`; `acceleration()` passes `inp.emergency` to `brakeForce` (both the moving branch and the static-hold branch read the same flag). The static-hold branch (physics.ts ~89–97) **does** consult `brakeForce` — it uses the returned `fBrake` to decide whether the train is held (`|fDrive| ≤ fBrake ⇒ held`) — so threading `emergency` in is **not** a no-op there. The correct property is monotonic and safe: because `brakeEmergencyDecel (1.3) > brakeServiceDecel (0.9)`, emergency yields a holding force **≥** service, so a train held under service is **always** held under emergency (never worse); and on a grade steep enough that a service hold would slip, emergency correctly holds it. `simulation.ts`: `SimInputs` gains a required `emergency: boolean`; `step()` threads it into the `AccelInputs` it builds. The `brakeActual` lag logic is unchanged — `emergency` selects the *decel scale*; the build-up still rides `brakeActual` toward the `brake=1` demand per `BRAKE_LAG`.

Default `emergency = false` on `brakeForce` keeps the function call API backward-safe, but per the brief's preference for **required fields + defaults over optionals** under `exactOptionalPropertyTypes`, `AccelInputs.emergency` and `SimInputs.emergency` are *required*. The single mechanical ripple is adding `emergency: false` to the literals in `physics.test.ts` (the `AccelInputs` in the RK4 oracle and every `SimInputs` in `drive(...)` call sites) — no assertion weakened. Why a flag and not `brake=1`: `brake∈[0,1] × serviceDecel=0.9` can never reach `1.3 m/s²`; emergency must be a genuinely distinct deceleration with its own closed-form stop distance `v²/(2·1.3)`.

### 2.8 AWS/TPWS future seam (typed no-op stub)

The cheapest shape Phase 2 can extend without rework — a reasons-shaped output and one pure fold point, homed under `src/sim/safety/`. No `AwsState`/`armed` machinery beyond what the seam needs:

```ts
// src/sim/safety/aws.ts
import type { PenaltyReason } from "../controls";
import type { SimState } from "../simulation";
export interface AwsOutput { reasons: PenaltyReason[]; } // empty now; Phase 2 returns "AWS"/"TPWS"
export function tickAws(_state: SimState, _dt: number): AwsOutput {
  return { reasons: [] }; // Phase-1 no-op pass-through (the fold point)
}
```

`tickSafety` consumes `aws.reasons` at the exact point DSD adds its reason and feeds the **same** `penaltyReasons` sink — proving the seam compiles and is shaped right. Phase 2 introduces whatever state the deadline/warning machine needs behind `tickAws` and starts returning reasons, with no change to the latch or `resolveInputs`.

### 2.9 The structured HUD

`buildHudView` is **pure and lives in `controls.ts`** (so it is unit-tested without any DOM dependency); `createHud` in `src/ui/hud.ts` is a thin, untested DOM writer:

```ts
// buildHudView in controls.ts
export interface HudView {
  speedMph: number; limitMph: number;
  reverser: "FWD" | "OFF" | "REV";
  powerNotch: number; powerMax: number;            // e.g. 2 of 4
  brakeLabel: string;                              // "RELEASE" | "STEP 1" | "STEP 2" | "FULL SERVICE" | "EMERGENCY"
  brakeDemandPct: number; brakeActualPct: number;  // show BOTH — the ~1.5–2 s lag must be visible
  dra: boolean; dsdWarning: boolean; penalty: boolean;
  nextStop: string; chainage: number;
}
export function buildHudView(state, c, s, route): HudView; // PURE, tested (powerMax from POWER_NOTCHES)

// createHud in src/ui/hud.ts
export function createHud(parent: HTMLElement): { update(v: HudView): void }; // DOM, untested
```

**The load-bearing split, stated crisply:** `buildHudView` is pure and **is** tested — mph (`|speed|·2.236936`), next-stop lookup (first station ahead of `chainage` in `dir`; when there is no station ahead — end of line — `nextStop = "— (end of line)"`, and the array lookup is guarded for `noUncheckedIndexedAccess` so the possibly-`undefined` element is handled, never dereferenced blindly), brake label from `brakeStep`, demand-vs-actual %, and the lamp booleans.

**Demand vs actual, and the next-stop heading, made explicit.** `brakeDemandPct` shows the **resolved brake demand** — the `brake` value out of `resolveInputs`, i.e. including any penalty- or emergency-forced full application — *not* the bare lever fraction; `brakeActualPct` shows `brakeActual` (the lagged pneumatic build-up). So under penalty the displayed demand reflects the forced full-service even if the lever sits lower, and at spawn both read **FULL SERVICE** (lever = FULL SERVICE per R5, and `brakeActual = 1`). The next-station scan picks "next station ahead" using `lastDir` (the retained heading), so it is well-defined even when the reverser is OFF (which leaves `lastDir` intact, §2.2/§2.4).

**`noUncheckedIndexedAccess` audit — every new array index Phase 1 introduces is guarded.** The next-station scan in `buildHudView` is the **only** new *array* index site: it iterates `route.stations` and the result is `T | undefined`, handled by the end-of-line fallback above (never blind-dereferenced). The other "lookups" are deliberately **not** array indexing and so incur no `noUncheckedIndexedAccess` obligation: the brake label is computed from `brakeStep` by a total `switch`/conditional (RELEASE / STEP 1 / STEP 2 / FULL SERVICE / EMERGENCY), not an array index; the power notch is rendered as `powerNotch of powerMax` (arithmetic, no index); the inline keymap (§2.3) is an object-literal lookup keyed by `KeyboardEvent.code` whose miss yields `undefined` and is filtered before building the intent, not an array index. No Phase-1 lookup site beyond `nextStop` indexes an array, so `nextStop` is the single guarded site. **Only its DOM rendering is the untested adapter** — `createHud` builds per-field `<span>`s once and patches `textContent`/lamp classes (amber for DSD warning + DRA set, red for penalty) on `update`. No jsdom dependency is incurred. The HUD is a sibling DOM overlay, not routed through `SceneHandle`.

### 2.10 What stays in the thin, untested `main.ts`

Only impure glue, no decision logic — explicitly the **deliberately-untested adapter surface**:
- create scene/route/spec/HUD and initial `SimState`/`ControlState`/`SafetyState`.
- `keydown` (`if (e.repeat) return`)/`keyup`/`blur` listeners; the per-frame edge set; the inline keymap lookup (§2.3).
- the rAF loop and **the only wall-clock:** `dt = min((now - last) / 1000, 0.05)`.
- per-tick orchestration (calls in order, no branching):

```
read edge set → intent = intentFromKeys(edgeSet)            // inline lookup
controls = reduceControls(controls, intent, state)
safety   = tickSafety(safety, intent, state, dt, tickAws(state, dt))
inputs   = resolveInputs(controls, safety, ADHESION.wetNight)
state    = step(spec, route, state, inputs, dt)
scene.render(state.chainage)
hud.update(buildHudView(state, controls, safety, route))
clear edge set
```

`mu` stays hardwired `ADHESION.wetNight` (no env var, no flag). Every branch that could be wrong lives in a pure, tested function; `main.ts` contains no control/safety/HUD arithmetic.

---

## 3. File-level impact map

| File | Action | Why |
|---|---|---|
| `src/sim/controls.ts` | **NEW (pure)** | The whole control/safety/HUD-derivation brain: types, constants, `reduceControls`, `tickSafety`, `resolveInputs`, `buildHudView`, interlock predicates, shared penalty latch. Unit-tested home of all driving/safety decisions. |
| `src/sim/safety/aws.ts` | **NEW (pure)** | Typed AWS/TPWS no-op stub (`AwsOutput.reasons`, `tickAws`) feeding the shared latch. The Phase-2 home + seam, no behaviour. |
| `src/ui/hud.ts` | **NEW (view, untested)** | Thin `createHud` DOM writer (per-field spans + lamp classes). No logic. |
| `src/sim/physics.ts` | **CHANGE** | `brakeForce` gains `emergency = false` selecting `brakeEmergencyDecel`; `AccelInputs` gains required `emergency`; `acceleration` threads it to both `brakeForce` calls. The one real pure-core gap; single call site. |
| `src/sim/simulation.ts` | **CHANGE** | `SimInputs` gains required `emergency: boolean`; `step` threads it into the `AccelInputs` it builds. No new `SimState` fields, no latches in core. |
| `src/main.ts` | **REWRITE (thin, untested)** | Replace the continuous ramp + inline reverser with the pure pipeline wiring (§2.10) + the inline keymap/edge-set. |
| `test/physics.test.ts` | **CHANGE (fixtures)** | Add `emergency: false` to the `AccelInputs` literal (RK4 oracle) and every `SimInputs` literal in `drive(...)` call sites. Mechanical; no assertion weakened. |
| `test/controls.test.ts` | **NEW** | Interlocks (incl. power-inhibit isolation O15a–e, reverser power+stand O16), reverser/DRA-at-stand, brake stepping O17, EMERGENCY latch O19, simultaneous-edge resolution O20, resolver truth table, DSD→penalty→ack (incl. positive-DSD O18), frame-rate independence, AWS stub pass-through, `buildHudView` projection (§4, O3–O20). |
| `test/emergency.test.ts` | **NEW** | Emergency stop-distance analytic oracle + emergency-shorter-than-service + emergency static-hold ≥ service (§4, O1–O2, O21). May fold into `physics.test.ts`. |
| `index.html` | **UNCHANGED** | HUD builds its own DOM overlay; `<div id="app">` suffices. |
| `src/render/scene.ts`, `src/sim/route.ts`, `src/sim/train.ts` | **UNCHANGED** | Render seam, route, and tuning constants (`brakeEmergencyDecel` already present) untouched. |
| `package.json` | **CHANGE** | Version bump `0.1.0 → 0.2.0` (new feature). No deps. |

No `src/input/` directory is created — the keymap is inline in `main.ts`.

---

## 4. Acceptance criteria / test oracles (the build run's definition of done)

All in the existing pure Vitest idiom (`environment:"node"`, no DOM), reusing the `drive(state, inputs, seconds)` helper. Tests are written **first**; modules are shaped to satisfy them. `npm run check` green, zero TS warnings, is the gate.

**Emergency decel — the new pure physics path**
- **O1 (analytic stop distance).** Strip Davis (`davisA/B/C = 0`); `v0 = 20`; `mu = ADHESION.dry` (so the brake is **not** adhesion-limited — see R3 caveat in §5); `{ notch:0, brake:1, dir:1, emergency:true, mu }`. **Start from a FULLY-APPLIED brake** — construct the start state `{ chainage:0, speed:v0, brakeActual:1, time:0 }` (exactly as the existing braking-distance oracle does at `physics.test.ts:109`) so the ~1.5 s brake build-up lag (`BRAKE_LAG.buildTau`) does **not** corrupt the closed-form match; from `brakeActual=0` the ±3% analytic tolerance is unreachable. Drive to rest: `end.speed < 1e-3` and `end.chainage ∈ [0.97, 1.03] × v0²/(2·brakeEmergencyDecel)`.
- **O2 (emergency strictly shorter than service).** Same setup — including the **fully-applied start state** `{ chainage:0, speed:v0, brakeActual:1, time:0 }` so neither run pays the build-up lag — `emergency:false` (full service) vs `emergency:true`: emergency stop distance `<` service stop distance, ratio ≈ `0.9/1.3` within tolerance. Proves the flag changes physics (guards against "faked as brake=1").
- **O21 (emergency static-hold ≥ service).** Pins the `acceleration()` static-hold property (§2.7): at a stand on the **steepest grade the route reaches**, with no power (`notch:0`) and a fully-applied brake (`brake:1, brakeActual:1`), an EMERGENCY application (`emergency:true`) holds the train at least as well as full service. Drive a few seconds from `{ speed:0, brakeActual:1 }` on that grade: `emergency:true` keeps `|speed| ≈ 0` (within `1e-3`) whenever `emergency:false` does, and on any grade where service would slip, emergency still holds (`|speed| ≈ 0`). Because `brakeForce`'s emergency decel (1.3) ≥ service (0.9), the holding force is never worse — so the static-hold branch is correctly monotonic, not a no-op.
- **Regression.** The existing `brakeServiceDecel` braking-distance oracle (`physics.test.ts:109`) and the RK4 oracle still pass with `emergency:false`.

**Interlocks — `resolveInputs`**
- **O3 (no power against any applied brake — vary ONLY `brakeStep`).** Hold the all-clear fixture (`reverser:"FWD"`, `dra:false`, no penalty) with `powerNotch = POWER_NOTCHES`, and vary only `brakeStep`: for every `brakeStep ∈ {1,2,3,EMERGENCY}`, `resolveInputs(c, s, mu).notch === 0`; with `brakeStep = 0`, `notch === 1`. Then a driving oracle: from `createInitialControls()` (brake = FULL SERVICE, R5) at rest, drive 10 s flat → `|speed| < 1e-6`, no creep.
- **O4 (reverser OFF inhibits power).** `reverser:"OFF", powerNotch:4, brakeStep:0` ⇒ `resolveInputs(c, s, mu).notch === 0`; `dir === lastDir` (still `1|-1`, never `0`).
- **O5 (exact fractions).** Via `resolveInputs(c, s, mu)`: `powerNotch:2 → notch === 0.5`; `brakeStep:2 → brake === 2/3`; `brakeStep:EMERGENCY → brake === 1 && emergency === true`.

**Reverser & DRA interlocks — `reduceControls`**
- **O6 (reverser only at a stand).** With `state.speed = 5`, a `reverserRev` intent leaves `reverser`/`lastDir` unchanged; at `speed = 0` it flips to REV with `lastDir = -1`. Composed through `step`: FWD power → +chainage/`speed>0`, REV power → −chainage/`speed<0`.
- **O7 (DRA inhibits power; cancel restores).** `dra:true, powerNotch:4, brakeStep:0` ⇒ `resolveInputs(...).notch === 0`; toggle DRA off at a stand ⇒ `notch === 1`; a DRA toggle attempted while moving is rejected (state unchanged).

**DSD / shared penalty latch — `tickSafety` (+ `resolveInputs` + `step`)**
- **O8 (frame-rate independence + no wall-clock).** With the train **moving** (`|state.speed| > STAND_EPS`) and **no `vigilancePing`**, advance the **same total elapsed time = `DSD_PERIOD + a few seconds`** (enough to cross *both* the warn window and the penalty threshold) in two `dt` regimes — one large `dt` call vs many small `dt` calls — with identical inputs (`state.speed`, `aws`, no ping) on every step. **End-equality:** the two regimes yield equal `vigilanceTimer` (within floating-point tolerance), equal `dsdWarning` (bool), and equal `penaltyReasons` (**EXACT `Set` equality**). **Crossing assertions (both regimes):** `dsdWarning` flips `true` at the warn crossing (`vigilanceTimer ≤ DSD_WARN_WINDOW`), and `"DSD"` latches into `penaltyReasons` at the penalty crossing (`vigilanceTimer ≤ 0`) — so the oracle advances *across* both thresholds rather than stopping short, catching a `<` vs `<=` / ordering / stateful-latch bug. **The float tolerance applies to the numeric `vigilanceTimer` only; `dsdWarning` and `penaltyReasons` are compared exactly.** This pins that timed safety integrates purely off `dt` and is independent of how the frame budget is sliced (R4). **Complementary static guard (the other half of O8):** a grep over `src/sim` and `src/input` finds no `Date`/`Math.random`/`performance.now`/`setTimeout` (the no-wall-clock definition-of-done).
- **O9 (timeout → penalty → stop — train must be moving, on the LEVEL).** Drive the train **moving** (`|state.speed| > STAND_EPS`, since the vigilance countdown is motion-gated — §2.6 step 2) with no `vigilancePing` for `DSD_PERIOD` s: `dsdWarning` becomes true at the warn window, then `penaltyReasons` contains `"DSD"`. Companion check: a *parked* train (`|state.speed| ≤ STAND_EPS`) ticked for the same elapsed time holds its timer and **never** latches DSD. Penalty active ⇒ `resolveInputs` forces `brake=1, emergency=false, notch=0`; driving from speed **on a LEVEL stretch (zero grade)** brings the train to `|speed| ≈ 0` cleanly, with no gradient roll-back to confound the assertion.
- **O10 (ack at a stand releases the latch — the AVOID-an-un-releasable-latch oracle).** While stopped (`|state.speed| ≤ STAND_EPS`) and penalty-latched, an `acknowledge` intent empties `penaltyReasons`; afterwards `resolveInputs` with full power + no brake yields `notch === 1` (power available again). (The vigilance timer is left holding because the train is at a stand — §2.6 step 2 — so the countdown only resumes once it moves again; a `vigilancePing` resets it to `DSD_PERIOD` if the driver acts.)
- **O11 (ack while moving does NOT release).** Penalty latched, `state.speed = 5`, `acknowledge` ⇒ `penaltyReasons` still contains `"DSD"`.
- **O12 (penalty ≠ emergency decel, on the LEVEL).** Under penalty, `resolveInputs(...).emergency === false` and `brake === 1` (full service, not slam) — pins the brief's §5 semantics. The driving leg runs on a **LEVEL stretch (zero grade)** so the full-service penalty brake brings the train to `|speed| ≈ 0` without gradient roll-back confounding the assertion.
- **O18 (positive DSD — periodic pings never penalise).** A `SafetyState` that receives a `vigilancePing` strictly more often than every `(DSD_PERIOD − DSD_WARN_WINDOW)` seconds — i.e. with a ping interval `< 53 s` for the 60/7 constants, e.g. ping every **50 s** — while the train is **moving** (`|state.speed| > STAND_EPS`), keeps `dsdWarning` **false** AND `penaltyReasons` **empty** throughout a `2 × DSD_PERIOD` run. (The `< (DSD_PERIOD − DSD_WARN_WINDOW)` bound is what guarantees each ping resets the timer before it can even enter the warning window, so the warning lamp never lights and the latch never trips.) The positive complement of O9 — pins the intended deadman behaviour (§2.3/§2.6) from the safe side, so an attentive driver is never penalised.
- **O19 (EMERGENCY latches — release only at a stand).** A driver-commanded EMERGENCY (`brakeStep = BRAKE_EMERGENCY`) **cannot be released while moving:** with `|state.speed| > STAND_EPS`, a `brakeDown` intent leaves `brakeStep === BRAKE_EMERGENCY` (unchanged). Once `|state.speed| ≤ STAND_EPS`, the same `brakeDown` decrements to `BRAKE_FULL (3)`. (Pins D18; complements O17, which covers the service-step decrements that are always free.)

**Simultaneous / conflicting edges — `reduceControls`**
- **O20 (one-frame conflict resolution).** Pins §2.3 ordering: an opposing `powerUp`+`powerDown` in the same intent ⇒ `powerNotch` unchanged; an opposing `brakeUp`+`brakeDown` ⇒ `brakeStep` unchanged; an `emergency` edge together with `brakeDown` ⇒ `brakeStep === BRAKE_EMERGENCY` (emergency overrides the brake edges); two reverser edges in one frame (e.g. `reverserFwd`+`reverserRev`) ⇒ `reverser`/`lastDir` unchanged. (Pins D19.)

**AWS/TPWS stub — Phase-2 seam**
- **O13 (pass-through).** `tickAws(...).reasons` is empty for arbitrary inputs; a full `tickSafety` with the stub adds no reasons beyond DSD; a `SafetyState` driven with vigilance pings stays penalty-free.

**HUD projection — `buildHudView` (pure)**
- **O14.** `speedMph = |speed|·2.236936`; `brakeDemandPct` shows the **resolved brake demand** (the `brake` out of `resolveInputs`, including any penalty/emergency-forced full application) and `brakeActualPct` shows `brakeActual`, so `brakeDemandPct ≠ brakeActualPct` mid-build-up (lag visible) and under penalty the displayed demand reflects the forced full-service even if the lever sits lower; **at spawn both read FULL SERVICE** (lever = FULL SERVICE per R5, `brakeActual = 1`); `reverser`/`brakeLabel` correct (RELEASE / STEP 1 / STEP 2 / FULL SERVICE / EMERGENCY); `powerMax === POWER_NOTCHES` (from the constant, not `spec` — `buildHudView(state, c, s, route)` takes no `spec`); lamp booleans (`dra`, `dsdWarning`, `penalty`) match control/safety state; `nextStop` = first station ahead in `lastDir` (the retained heading, so it is well-defined when the reverser is OFF); with **no station ahead** (end of line), `nextStop === "— (end of line)"`. (DOM rendering in `createHud` is the declared untested adapter.)

**Power-inhibit isolation — `resolveInputs` (one inhibitor at a time)**

These pin each OR-term of the power-inhibit composition (§2.5 step 3) *independently*, so no single inhibitor can silently stop working behind another. Baseline fixture = all-clear: `reverser:"FWD"`, `brakeStep:0`, `dra:false`, no penalty, `powerNotch:POWER_NOTCHES`. Each asserts `resolveInputs(c, s, mu).notch`:

- **O15a (brake-only inhibits).** From the baseline, set `brakeStep:1` (only) ⇒ `resolveInputs(c, s, mu).notch === 0`.
- **O15b (reverser-OFF-only inhibits).** From the baseline, set `reverser:"OFF"` (only) ⇒ `resolveInputs(c, s, mu).notch === 0`.
- **O15c (DRA-only inhibits).** From the baseline, set `dra:true` (only) ⇒ `resolveInputs(c, s, mu).notch === 0`.
- **O15d (penalty-only inhibits).** From the baseline, latch a penalty (only, `penaltyReasons ∋ "DSD"`) ⇒ `resolveInputs(c, s, mu).notch === 0`.
- **O15e (all-clear passes power).** The unmodified baseline (`FWD`, no brake, no DRA, no penalty) ⇒ `resolveInputs(c, s, mu).notch === notchFraction(POWER_NOTCHES) === 1`.

**Reverser power+stand interlock — `reduceControls`**
- **O16 (reverser change needs `powerNotch === 0` AND at a stand).** At a stand (`state.speed = 0`) with `powerNotch > 0`, a `reverserRev` intent is **rejected** (`reverser`/`lastDir` unchanged) — power-off is required even at rest. With `powerNotch === 0` at a stand, the same intent is **accepted** (flips to REV, `lastDir = -1`). (Complements O6, which holds power off and varies speed.)

**Brake stepping — `reduceControls`**
- **O17 (stepped brake + end gate).** From `brakeStep ∈ {0,1,2}`, a `brakeUp` yields `step + 1` and **never** EMERGENCY. From `FULL (3)`, a `brakeUp` yields `EMERGENCY (4)` (the end gate). `brakeDown` decrements exactly one step and clamps at `0` (never below). The dedicated `emergency` edge slams `brakeStep` directly to `EMERGENCY (4)` from any step. (Pins D14. The EMERGENCY→FULL release is gated at a stand — pinned separately by O19.)

**Definition of done:** `npm run check` green; oracles O1–O21 present and passing (including the O15a–e isolation set, the O9 parked-train companion, the O18 positive-DSD complement, the O19 EMERGENCY latch, the O20 conflict-resolution set, and the O21 emergency static-hold property); **no `Date`/`Math.random`/`performance.now`/`setTimeout` anywhere under `src/sim` or `src/input`** (verifiable by grep); `main.ts` contains no control/safety/HUD arithmetic.

---

## 5. Residual risks

1. **EMERGENCY adhesion caveat (keep — not a bug).** EMERGENCY stays distinct from FULL SERVICE *only while not adhesion-limited*. At `ADHESION.wetNight` (μ=0.2) the adhesion limit is `0.2·160000·9.80665/meff ≈ 1.81 m/s²`, which exceeds 1.3, so emergency is **not** clipped at the signature setting — but at very low grip the limit drops below 1.3 and emergency ≈ service. This is physically correct, not a defect; a future reader must not "fix" it. The analytic oracle (O1/O2) therefore uses `ADHESION.dry` (limit ≈ 2.72 m/s² > 1.3), exactly as the existing braking-distance oracle does, so the `v²/(2·1.3)` check stays valid.
2. **Penalty-release ordering (the load-bearing seam).** Phase 1's release rule — "clear on an ack edge AND at a stand" — is simple, but the *empty-set-clears-latch* invariant and the Phase-2 extension (reasons that persist after ack must fail a "source no longer demands it" predicate, kept as a forward-looking note in §2.6) are the one place a future bug could strand the player or make the latch un-releasable. Mitigated by O10/O11 and the documented contract in `controls.ts`; the forward note names the persisting-reason case explicitly so Phase-2 AWS slots in without touching the release machine.
3. **`exactOptionalPropertyTypes` friction.** Making `emergency` a required field ripples into existing `AccelInputs`/`SimInputs` literals (physics test + `main`). Mitigated: it's a required boolean with an obvious `false`; the impact map lists every literal to touch — small and mechanical.
4. **Untested adapter surface** (inline keymap, edge-set construction, rAF, DOM writer) can hide wiring bugs no oracle catches — e.g. not clearing the edge set per frame → double-stepping. Mitigated by keeping it to a few lines (`if (e.repeat) return` + clear-per-frame is the whole logic) and pushing every decision into pure functions; the residual is intentional and named. Manual smoke-test via `vite dev`.
5. **Shared epsilon / feel tuning.** `STAND_EPS = 0.3` is reused for reverser, DRA, EMERGENCY release, and latch release — coherent but tuning one tunes all; `DSD_PERIOD`/`DSD_WARN_WINDOW` are guesses, and linear `notch/Nmax` gives even effort steps that may feel un-prototypical against a real motor curve (the constant-power taper in `tractiveEffort` softens this). All are single-point named constants; oracles assert behaviour *relative to* the constants, not magic numbers, so tuning won't break tests. None is a blocker.
6. **Phase-2 coupling debt.** The latch's `Set<PenaltyReason>` and the `tickAws` seam are designed for AWS/TPWS but unexercised in Phase 1; O13 is the only guard that the seam stays shaped right. Flagged as the PLN's "on-ramp" — kept minimal, not over-built.

---

## 6. Decision log

| # | Decision | Rationale |
|---|---|---|
| D1 | **Parallel pure `ControlState`/`SafetyState`** stepped alongside `step`, not fields on `SimState`. | Zero blast radius on physics tests; safety gets its own pure module + oracles; preserves the `step` contract (one `SimInputs`+`dt`, no latches). Brief Q3 Option A. |
| D2 | **Integer notch/brake indices** as source of truth; fractions derived. | Exact, oracle-friendly fractions ({0,.25,.5,.75,1}); stepped feel; no float drift in a `notch===0.5` assertion. |
| D3 | **Emergency via a separate `emergency` channel**, default `false`, not by reinterpreting `brake=1`. | `brake∈[0,1] × 0.9` can't reach 1.3 m/s². Distinct decel needs its own analytic oracle; default-false preserves the braking-distance + RK4 oracles. Brief Q1 / R3. |
| D4 | **Reverser neutral lives only in the control layer**; `SimInputs.dir` stays `1\|-1` with `lastDir` retained through OFF. | Widening `dir` to `0` breaks `acceleration`/`step` and every existing test. OFF is modelled as `notch→0`. Brief Q2 / D5. |
| D5 | **Forced brake = raise `brake` / zero `notch` / set `emergency` floats before `step`**, not a new safety flag in `SimInputs`. | Keeps safety testable in its own pure module; `step` stays a dumb integrator owning `brakeActual` lag. The only new `SimInputs` field is `emergency`, a genuine physics input. Brief Q4. |
| D6 | **One pure `resolveInputs`** with a fixed pipeline (power-inhibitors OR, brake-demands `max`). | One truth-table oracle instead of logic smeared across the rAF closure — the brief's "on-ramp to nowhere" guard. Order-independent by construction. Brief Q5 / R2. |
| D7 | **One `penaltyReasons: Set` + one ack**, shared by DSD now and AWS/TPWS later; documented per-reason clearability contract. | The load-bearing Phase-2 seam (Brief Q6). Each subsystem owns only its reason; latch clears only when empty; releasability is an explicit oracle. R1. |
| D8 | **Penalty brake = FULL SERVICE, not emergency.** | Brief §5 ("full-service, latched"). Reserves emergency decel for the driver's deliberate EMERGENCY lever; O12 pins it. |
| D9 | **All timed safety advances off explicit `dt`**; wall-clock confined to the `main.ts` rAF pacer. | Determinism; reproducible vigilance/penalty oracles. A frame-rate-independence oracle (O8) plus the static no-wall-clock grep guard the boundary directly. Brief Q11 / R4. |
| D10 | **Keymap inline in `main.ts`**, not a separate `src/input/keymap.ts`. | The keymap is a ~20-line decision-free lookup; the only untested part (edge-set construction) is untested either way. "Keep the split minimal / don't predict the future" (Brief §2, §3). Trims one speculative module. |
| D11 | **AWS stub under `src/sim/safety/aws.ts`.** | Signals the Phase-2 home for the AWS/TPWS state machine without over-building it; keeps `controls.ts` focused. |
| D12 | **HUD split: pure `buildHudView` (in `controls.ts`, tested) + thin `createHud` (untested DOM).** | Pushes the load-bearing projection (mph, demand-vs-actual, lamp booleans) into a node-testable function with no jsdom dependency; the DOM writer is trivial glue. Shows brake demand vs `brakeActual` (Brief Q8). |
| D13 | **`createInitialControls` seeds `brakeStep = FULL SERVICE`** to match `brakeActual=1`. | Lever, demand, and actual agree at spawn; no released-lever-vs-applied-brake mismatch, no roll-away on frame 1. Brief Q7 / R5. |
| D14 | **EMERGENCY reachable only by stepping past FULL (an end gate)**, never by single-keypress overshoot from a partial step. | Pins the prototypical "past an end gate" brake behaviour the brief calls out (§5). |
| D15 | **Fixed 4 power notches / brake = RELEASE + 3 service applications (STEP 1, STEP 2, FULL SERVICE = index 3) + EMERGENCY (index 4); `mu` hardwired `wetNight`; no env vars/flags.** | Sane prototypical defaults; don't over-parameterise (Brief Q9); minimise config surface (quality brief). Index 3 **is** FULL SERVICE — there is no separate fourth service step. |
| D16 | **Reverser change requires `powerNotch === 0` AND `\|speed\| ≤ STAND_EPS`** (power off *and* at a stand), not just at a stand. | The brief names a power-off-plus-stand reverser interlock; changing direction under power is unsafe even momentarily at rest. Pinned by O16. |
| D17 | **DSD vigilance countdown is motion-gated:** the timer only decrements while `\|speed\| > STAND_EPS`; at a stand it holds. | A correctly parked train must never be penalty-braked for inactivity. Pinned by O9 (moving) + its parked-train companion. |
| D18 | **EMERGENCY brake LATCHES:** a `brakeDown` from EMERGENCY(4)→FULL(3) is rejected unless `\|speed\| ≤ STAND_EPS`; service-step brakeDown always decrements freely. | Mirrors the penalty latch and real emergency-brake behaviour — you don't release an emergency application mid-stop; it releases only once the train is at a stand. Pinned by O19. |
| D19 | **Simultaneous one-frame edge conflicts are resolved deterministically:** `emergency` overrides all brake edges; opposing `brakeUp`+`brakeDown` cancel; opposing `powerUp`+`powerDown` cancel; multiple reverser edges are all ignored. | A frame carrying contradictory edges must be well-defined, not order-dependent. Pinned by O20. |
| D20 | **DRA is a separate latching power-inhibit, NOT a penalty reason and NOT a latch input.** The DSD/vigilance device alone feeds the one shared penalty latch (with one ack); the DRA only inhibits power (§2.5 step 3), applies no brake, and is cancelled manually at a stand. `PenaltyReason` stays `DSD\|AWS\|TPWS`. | A reminder appliance is not a protection event; routing it through the shared latch would conflate a power-inhibit with a brake-demanding penalty. Keeps §1, §2.5, §2.6, and the `PenaltyReason` type in agreement. |
| D21 | **Phase-1 signatures carry only the parameters they use** (`noUnusedParameters`): `resolveInputs(c, s, mu)` (no `SimState`), `buildHudView(state, c, s, route)` (no `spec`; `powerMax` from the `POWER_NOTCHES` constant). The AWS seam keeps `tickAws(_state: SimState, _dt)` with `SimState` imported, so Phase 2 need not change the signature. | Strict TS (`noUnusedParameters`) would reject unused params; the `_`-prefixed AWS seam is the one exception, kept shaped for Phase 2 with the type imported so it type-checks. |
| D22 | **Emergency is threaded into the static-hold branch for a real, monotonic reason, not as a harmless no-op.** `acceleration()`'s static-hold branch (physics.ts ~89–97) uses `brakeForce`'s returned hold force; since emergency decel (1.3) ≥ service (0.9), emergency holds **≥** service everywhere — a service-held train is always emergency-held, and a grade that would slip under service is held under emergency. | Corrects the prior false "decel only matters while moving" claim. Pinned by O21. |

Relevant paths: `C:\language\mdtrain2\src\sim\physics.ts:59` (`brakeForce`), `C:\language\mdtrain2\src\sim\simulation.ts:8` (`SimInputs`), `C:\language\mdtrain2\src\sim\train.ts:18` (`brakeEmergencyDecel`), `C:\language\mdtrain2\test\physics.test.ts:109` (braking-distance oracle), `C:\language\mdtrain2\src\main.ts` (rewrite target).

### Review round 1 resolutions

Team-lead review V1→V2; each resolution and its rationale:

- **F1 (brake-fraction definition).** Defined `brakeFraction(brakeStep) = clamp01(brakeStep / BRAKE_SERVICE_STEPS)`, so FULL(3)→1 and EMERGENCY(4)→clamp01(4/3)=1 — both map to brake demand 1. *Rationale:* removes ambiguity over EMERGENCY's demand; the EMERGENCY physics difference comes solely from the separate `emergency` boolean, not the fraction. Load-bearing for O5/O12.
- **F2 (O3 well-specified + isolation oracles).** O3 now holds `FWD`/no-DRA/no-penalty and varies only `brakeStep`; added O15a–e pinning each power-inhibit OR-term independently plus the all-clear pass-through. *Rationale:* a single inhibitor can't silently stop working hidden behind another.
- **F3 (third reverser interlock).** Reverser change requires `powerNotch === 0` AND at a stand (§2.4 step 1, D16, O16). *Rationale:* the brief names a power-off-plus-stand interlock; direction change under power is unsafe even at rest.
- **F4 (motion-gated vigilance).** The vigilance timer only decrements while moving (§2.6 step 2, D17, O9). *Rationale:* a correctly parked train must never be penalty-braked for inactivity.
- **F5 (O8 reframed).** O8 is now a frame-rate-independence property (same elapsed time, different `dt` slicing ⇒ same `SafetyState`), with the static no-wall-clock grep as the complementary guard. *Rationale:* the old "call twice, expect equal" is vacuous for a pure function.
- **F6 (brake-stepping oracle).** Added O17 pinning D14's stepping/end-gate/emergency-edge behaviour. *Rationale:* D14 previously had no oracle.
- **Minor folds.** Simplified the AWS stub to `AwsOutput { reasons }` + `tickAws(state, dt)` (dropped `AwsState`/`armed`); dropped the redundant `|| intent.acknowledge` from the vigilance reset (ping covers reset; ack stays on penalty release only); demoted the per-reason "source no longer demands it" clause to a forward-looking Phase-2 note (kept the mandated Set + empty-set-clears-latch rule); specified `buildHudView`'s end-of-line `nextStop` and guarded the array lookup for `noUncheckedIndexedAccess`; added a note that interlocks read the one-frame-stale `state.speed` and no-creep still holds via `step()`'s through-zero snap. *Rationale:* fewer moving parts; each fold removes speculative machinery or pins a previously-implicit behaviour.

### Review round 2 resolutions

Team-lead review V2→V3 (these F-numbers are round-2 findings, independent of the round-1 list above):

- **F1/F3/F4/F7/F8 — no change (re-raised against a stale copy).** These were already resolved in V2 (brake-fraction definition, reverser power+stand interlock, motion-gated vigilance, AWS-seam shape, and the latch/empty-set contract); no edit needed.
- **F2 — start-state pinned.** O1/O2 now construct a fully-applied start state `{ chainage:0, speed:v0, brakeActual:1, time:0 }`, so the ~1.5 s `BRAKE_LAG.buildTau` lag cannot corrupt the closed-form `v²/(2·decel)` match (±3% tolerance reachable).
- **F4/F6 — positive DSD oracle + intended-behaviour note.** Added O18 (periodic pings while moving ⇒ never penalised) and a clarifying note (§2.3/§2.6) that vigilance resets on any control edge and a held lever produces no edges, so making no input for `DSD_PERIOD` while moving correctly trips the penalty — intended deadman behaviour, not a bug.
- **F5 — reverser-at-crawl residual note.** Added a one-line note (§2.4): `STAND_EPS` permits a reverser change at a crawl (≤ 0.3 m/s) in principle, but the `powerNotch === 0` interlock (D16) makes the through-zero snap active so the change happens at a true stand; benign, no new constant.
- **F11 — guards.** Confirmed (§2.9) that the next-station scan is the only new array-index site and is guarded for `noUncheckedIndexedAccess`; brake/power labels and the keymap are not array indexing and carry no such obligation.
- **F12 — citation.** Corrected the `brakeEmergencyDecel` citation from `train.ts:26` to `train.ts:18`; re-verified `physics.ts:59` (`brakeForce`) and `simulation.ts:8` (`SimInputs`) as correct.

### Review round 3 resolutions

Team-lead review V3→V4. **Round 3 found ZERO critical issues — the adversarial review loop has converged.** The items below are post-convergence refinements (strengthened oracles, two pinned-down behaviours, and clarifying folds), not corrections of defects:

- **F1 (O8 strengthened to cross the thresholds).** Rewrote O8 to advance the **same total elapsed time = `DSD_PERIOD` + a few seconds** across *both* the warn and penalty thresholds, via one large `dt` vs many small `dt`, asserting end-equality of `vigilanceTimer` (float tolerance), `dsdWarning` (bool), and `penaltyReasons` (**exact `Set` equality**), AND asserting `dsdWarning` flips at the warn crossing and `"DSD"` latches at the penalty crossing in both regimes. *Rationale:* the prior worked example never crossed the thresholds, so it could not catch a `<` vs `<=` / ordering / stateful-latch bug. The static no-wall-clock grep stays as the complementary half of O8.
- **F2 (EMERGENCY latches).** Decided and documented (D18, §2.4 step 3): EMERGENCY→FULL `brakeDown` is rejected unless at a stand; service-step decrements stay free. Added O19. *Rationale:* mirrors the penalty latch and real emergency-brake behaviour — you don't release an emergency application mid-stop.
- **F3 (simultaneous opposing edges).** Specified one-frame conflict resolution (D19, §2.3/§2.4): `emergency` overrides brake edges; opposing power/brake edges cancel; multiple reverser edges are all ignored. Added O20. *Rationale:* a frame with contradictory edges must be well-defined, not order-dependent.
- **F4 (narrowed the no-creep claim).** Reworded §2.0 to claim exactly what the through-zero snap provides: it prevents **power-induced** motion when a notch is suppressed and speed is at/crossing zero, and **does not** stop a brake-released, power-inhibited train rolling on a gradient (correct gravity physics, governed by the brake, not the interlock).
- **Minor folds.** F6 — O14/§2.9 now state the HUD shows resolved brake **demand** (incl. penalty/emergency) vs `brakeActual`, both reading FULL SERVICE at spawn. F7 — O14/§2.9 note `buildHudView` picks the next station ahead using `lastDir`, so it is well-defined when the reverser is OFF. F8 — O9/O12 now run on a **LEVEL** stretch so the brake stops the train cleanly without gradient roll-back. F9 — §2.7 originally claimed passing `emergency` into the static-hold branch was harmless (decel scale only matters while moving); **this was corrected in V5 by Codex#3** — the static-hold branch does consult `brakeForce`, and the true property is emergency-holds-≥-service (see the External review subsection below and O21).

### External (Codex) review resolutions

External-reviewer (Codex) findings on V4, all team-lead-accepted (V4→V5). **None were rejected.**

- **Codex#1 (DRA is NOT a latch reason) — accepted.** §1, §2.5, §2.6, and the `PenaltyReason` type now agree: the DSD/vigilance device alone feeds the one shared penalty latch (one ack); the DRA is a **separate latching power-inhibit** (no brake, not a `PenaltyReason`, never an input to the latch). `PenaltyReason` stays `DSD\|AWS\|TPWS`. Reworded the §1 "Safety now" bullet; added D20.
- **Codex#2 (strict TS — drop unused params + add missing import) — accepted.** Changed `resolveInputs` to `resolveInputs(c, s, mu)` (dropped the unused `SimState`) at its §2.5 definition and every oracle call-site (O3, O4, O5, O15a–e); changed `buildHudView` to `buildHudView(state, c, s, route)` (dropped unused `spec`; `powerMax` comes from the `POWER_NOTCHES` constant) in §2.9 and O14; added `import type { SimState } from "../simulation";` to the §2.8 `aws.ts` sketch so `tickAws(_state, _dt)` type-checks. Avoids `noUnusedParameters` failures. Added D21.
- **Codex#3 (correct the emergency/static-hold claim) — accepted.** The prior "harmless / decel only matters while moving" claim in §2.7 was **false**: `acceleration()`'s static-hold branch (physics.ts ~89–97) uses `brakeForce`'s hold force, and emergency decel (1.3) > service (0.9). Reworded §2.7 to the correct property (emergency holds ≥ service, so a service-held train is always emergency-held and a grade that would slip under service is held under emergency); added oracle **O21**; added D22.
- **Codex#4 (tighten O18) — accepted.** O18's ping interval is now specified as `< (DSD_PERIOD − DSD_WARN_WINDOW)` (i.e. `< 53 s` for 60/7), e.g. ping every **50 s**, asserting `dsdWarning` stays false AND `penaltyReasons` stays empty over a `2 × DSD_PERIOD` run. The earlier `(DSD_PERIOD − margin)` bound was too loose to guarantee the warning window is never entered.
- **Codex#5 (consistent brake labels) — accepted.** Removed the double-count. The model is exactly index 0 = RELEASE, 1 = STEP 1, 2 = STEP 2, 3 = FULL SERVICE (= `BRAKE_FULL` = `BRAKE_SERVICE_STEPS`), 4 = EMERGENCY — i.e. RELEASE + 3 service applications (STEP 1, STEP 2, FULL SERVICE) + EMERGENCY. Fixed §1, the §2.2 `brakeStep` comment, the HUD `brakeLabel` comment (`RELEASE | STEP 1 | STEP 2 | FULL SERVICE | EMERGENCY`), the §2.9 audit prose, D15, and O14 so all agree that index 3 **is** full service. F11 — §2.3/§2.6 note the acknowledge key, being a control edge, also resets the vigilance timer via `vigilancePing` even on a tick where it does not release the latch (intended).
