# HLD — mdtrain2 "Grand World": living terrain, a grand route, a push to realism — V4

**Status:** DRAFT (V4) · Date 2026-06-15 · Target `C:\language\mdtrain2` (Phases 0-5 merged, v0.10.0). `package.json` 0.10.0 → **0.11.0**.
**Provenance:** V1 synthesis → V2/V3 = 2 adversarial review rounds (42 findings) → **V4 = + external Codex (gpt-5.5, xhigh) review** (10 findings, all valid, applied). Codex caught two implementation-sinkers the agent team missed: a single heightfield can represent neither the **viaduct** (it fills the valley solid) nor the **tunnel** bore (D20, two-surface model), and the camera yaw needs the **`+π` Three offset** to face down the line (D21). Plus: `camera.ts` un-folded (the fold made an ESM cycle, D1''); feature predicates use **macro relief only** with **bounded noise** (D24); env-texture color-spaces + `EquirectangularReflectionMapping` pinned (D23); lazy bloom reconciled with the **synchronous** `createScene` (D22); `O-RIBBON`/radius-floor **scoped to terrain-rendered routes** (so the retained `WESTFORD_EASTBANK` fixture is exempt).

The world is a flat green plane the rails lie *on*. This overhaul makes it a country the line **threads through** — cuttings that cut, embankments that carry, a river valley spanned by a **viaduct**, a headland bored by a **tunnel** — on a longer, seven-chapter **Grand Route**, lit with ACES tone-mapping, PBR materials, one real sun and a real sky. The governing instinct: **the world is geometry, and geometry is math.** Every metre of track, every terrain height, every prop's place, every "is this span a viaduct?" decision is a *pure function of route data in curvilinear coordinates*, computed and checked on Node with no GPU. Three.js is a thin, named, screenshot-verified projector that draws what the pure layer computed.

---

## 1. Scope & non-goals

### In scope
1. **A pure curvilinear spatial core** under `src/sim`, three small pure modules:
   - **`centerline.ts`** (path only): `centerlineAt(route,s) → Frame{x,y,z, tangent, up, heading, cant}`; `heightAt` (= ∫grade, analytic); `headingAt`; `planarPoseAt(route,s) → {x,z,heading}`; `placeOnCentreline(route,s,d) → {x,y,z, heading}` — the single placement primitive for every lineside object **and** set-piece structure (returns the *mathematical* track heading; Three facing offsets are applied render-side, D21).
   - **`terrain.ts`** (imports `centerline`): the **two-surface** model — `formationHeight(route,s)` (the rail/deck top) vs `terrainHeight(route,s,d)` (the ground), which coincide near the track normally but **diverge** in viaduct/tunnel bands; plus `reliefAt`/predicates (on **macro** relief), `anchorY`, `clampEye`.
   - **`camera.ts`** (imports both): `cabAttitudeTarget(cant,grade,scale) → {pitch,roll}` (pure clamp; eased render-side) and `eyePose(route,s,eyeD,eyeHeight) → {position, heading}` (placeOnCentreline + `clampEye`). *(Restored as its own module — folding it into `centerline.ts` created an ESM cycle, D1''.)*
2. **A real living landscape:** a build-once, seam-welded, sectioned **terrain-ground ribbon**; a **track ribbon** (rails/sleepers/ballast) always at `formationHeight` along the centreline; a **multi-arch viaduct** (deck at formation, piers to the natural valley floor) and a **tunnel** (hill ground + a bore cutout + portal/bore shell), both placed via `placeOnCentreline`; **sea/river** planes; every prop via `placeOnCentreline`+`anchorY`. The flat ground, cone "hills" and faked `addBank` cuttings are **deleted**.
3. **A push toward realism, free-first, tier-gated:** `ACESFilmicToneMapping` set on the renderer (applied once on either the direct-to-canvas or the `OutputPass` composer path — D17); a **color-space-correct** pipeline (D23); a **palette re-tune** of the existing emissives (D14); **procedural canvas textures + normal maps** (no committed image assets); a **gradient sky dome** (`fog:false`); a **tiny 16×16 procedural equirect env map** on `scene.environment` for cheap IBL ambient + wet-rail sheen (D5'); and — **tier-gated** — **one** eye-tracking shadow sun and an **optional, async-loaded** bloom pass. Everything scales down via `quality.ts`; nothing breaks if it's all off.
4. **A grand multi-chapter route:** `KINGSGATE_SEAHAVEN`, ~**14 km**, **7 chapters**, optional `viaducts[]`/`tunnels[]`/`terrainSeed`. `WESTFORD_EASTBANK` **retained as a test fixture** (physics/signalling only; never terrain-rendered).

### Non-goals
No 2-D track plan (D2). No physics change (1-D `gradeAt`; never imports the 3-D path — O17). No world→chainage inversion (curvilinear `(s,d)`). No chunked/streaming terrain (build-once sectioned ribbon + far-horizon band + fog — D8). No C1 easements (D11). No full scene-cube PMREM, water sim, modelled tunnel interior, god-rays, day/night cloud sim. No new npm dependency. No env vars, no feature flags. No migration/back-compat except keeping `WESTFORD_EASTBANK` as a fixture.

---

## 2. The design

### 2.0 Architecture in one breath
```
route data (1-D bands + optional viaducts/tunnels/terrainSeed)
   │
   ├─ physics.ts ── gradeAt(route,s)                                   [UNCHANGED 1-D path]
   │
   ├─ centerline.ts (PRIME SEAM) ── centerlineAt/heightAt/headingAt/planarPoseAt/placeOnCentreline
   │       └─ terrain.ts (imports centerline) ── formationHeight; terrainHeight(s,d); reliefAt; macro
   │             predicates viaductSpanAt/tunnelBoreAt; anchorY; clampEye
   └─ camera.ts (imports centerline + terrain) ── eyePose(route,s,eyeD,eyeHeight); cabAttitudeTarget

environment.ts ── …+ exposure, sunDir, sunColorPbr, bloomStrength
quality.ts     ── …+ shadowsEnabled, bloomEnabled, ribbonHalfWidth, terrainSegLen, terrainSubdiv,
                   attitudeScale; lodForDistance(d); ribbonVertexCount(tier,len)

render/* (IMPURE): read the pure layer; apply Three facing offsets + the cab-attitude ease; place & draw. No spatial math.
```
Everything left of `render/*` is pure, under `src/sim`, banned-token-free (integer-hash noise; trig allowed), oracle-tested. The **G3 glob is widened** to `src/sim/*.ts` + `src/render/quality.ts` (D6'); the G3 matcher **strips comments first** so a banned word in a comment can't false-positive (finding #10).

### 2.1 `src/sim/centerline.ts` (NEW, PURE, TESTED) — the path
```ts
export interface Frame { x:number; y:number; z:number; tangent:Vec3; up:Vec3; heading:number; cant:number; }
export function centerlineAt(route: Route, s: number): Frame;
export function heightAt(route: Route, s: number): number;            // ∫₀ˢ grade (analytic ⇒ C0)
export function headingAt(route: Route, s: number): number;            // tangent yaw, rad (heading=0 ⇒ tangent=+Z)
export function planarPoseAt(route: Route, s: number): { x:number; z:number; heading:number };
export function placeOnCentreline(route: Route, s: number, d: number): { x:number; y:number; z:number; heading:number };
```
- **Height** = `Σ grade_i·(seg∩[0,s])`, clamped; piecewise-linear ⇒ C0 (O1–O3).
- **Planar pose:** heading `ψ(s)=ψ₀+∫κ`; per segment compute the LOCAL arc displacement and **rotate by the entry heading ψ₀** before adding to the running pose (`x += (cosψ₀−cos(ψ₀+θ))/κ`, `z += (sin(ψ₀+θ)−sinψ₀)/κ`, `θ=κL,R=1/κ`); straight advances `L` along `ψ₀`. ψ carries across breakpoints ⇒ G1 tangent (O4, O5, O5b). **`lateralOffsetAt` is removed** — the model is an accumulated `(x,z,heading)` pose, not a single offset (finding #8); use `planarPoseAt`.
- **`placeOnCentreline(s,d)`** returns world `(x,y,z)` = `planarPose + d·right` (`right=(cos heading, −sin heading)`) with `y = centerlineAt(s).y`, and the **mathematical `heading`** (heading=0 ⇒ +Z). **Three facing offsets are render-side (D21):** a camera uses `heading+π`; a mesh modelled front-+Z uses `heading`; a rear-facing signal lamp uses `heading+π`. (O5c pins position incl. the left/right **sign**; O5d pins heading semantics.)
- **cant** = `clamp(CANT_GAIN·|κ(s)|·v_ref(s)², CANT_MAX)`, signed; `v_ref=speedLimitAt(s)`; **pinned `CANT_GAIN=0.08`, `CANT_MAX=0.105 rad`** (O6).
- `Math.sin/cos/sqrt/atan` allowed; no clock. Honest `z(s) ≤ s` on curves; physics never reads it (R6).

### 2.1b `src/sim/camera.ts` (NEW, PURE, TESTED) — the eye + cab attitude
```ts
export function cabAttitudeTarget(cant: number, grade: number, scale: number): { pitch:number; roll:number };
export function eyePose(route: Route, s: number, eyeD: number, eyeHeight: number): { x:number; y:number; z:number; heading:number };
```
- `eyePose` = `placeOnCentreline(s, eyeD)` lifted by `eyeHeight`, with `y = clampEye(route, s, eyeD, place.y + eyeHeight)`. **Pinned `EYE_HEIGHT=1.9`, `EYE_D=−0.5`.**
- `cabAttitudeTarget(cant,grade,scale) = { pitch: clamp(scale·atan(grade), ±PITCH_MAX), roll: clamp(scale·cant, ±ROLL_MAX) }`. **Pinned `ROLL_MAX=0.070`, `PITCH_MAX=0.052`, `attitudeScale=0.35`(desktop)/`0`(coarse & reduced-motion).** *Defensive ceilings:* in the live path `cant≤CANT_MAX` and grades are gentle, so `scale·CANT_MAX≈0.037<ROLL_MAX`; O16 exercises the ceilings with synthetic over-clamp inputs.
- **One pitch path (finding #3):** the camera's pitch is the **eased cab pitch + look.pitch only** — *not* a separate raw `atan(grade)` term. The render layer eases the live `(pitch,roll)` toward `cabAttitudeTarget` via `approach()` each frame and sets `camera.rotation = (easedPitch + lookPitch, heading + π + lookYaw, easedRoll)` (YXZ); `cabMount` takes the same eased pitch/roll, `heading + π`, no look offset. *(Module split resolves the ESM cycle: `centerline` is path-only, `terrain` imports `centerline`, `camera` imports both — D1''.)*

### 2.2 `src/sim/terrain.ts` (NEW, PURE, TESTED) — the **two-surface** living land
The key correction (Codex CRIT1, D20): the **track/deck** and the **ground** are two surfaces.
```ts
export function formationHeight(route: Route, s: number): number;          // rail/deck TOP (+ ballast). The train runs here, ALWAYS.
export function macroGround(route: Route, s: number, d: number): number;    // authored macro landform (valley/hill plateaus), NO noise
export function naturalGround(route: Route, s: number, d: number): number;  // macroGround + bounded seeded value noise
export function terrainHeight(route: Route, s: number, d: number): number;  // the GROUND surface (see below)
export function macroReliefAt(route: Route, s: number): number;             // macroGround(s,0) − formation(s)  [NO noise]
export function viaductSpanAt(route: Route, s: number): boolean;           // macroReliefAt < −VIADUCT_THRESH
export function tunnelBoreAt(route: Route, s: number): boolean;            // macroReliefAt >  TUNNEL_THRESH
export function anchorY(route: Route, s: number, d: number, clearance: number): number; // terrainHeight + clearance
export function clampEye(route: Route, s: number, eyeD: number, eyeY: number): number;  // ≥ terrainHeight(s,eyeD)+MIN_EYE_CLEARANCE
```
**The ground surface (D20):**
```
norm (not viaduct/tunnel): terrainHeight(s,d) = lerp( formation(s)+ballastShoulder(|d|), natural(s,d), w(d) )   // clean bed near track
viaduct band:              terrainHeight(s,d) = natural(s,d)                                                     // ground = the valley floor; NO ballast clamp
tunnel band:               terrainHeight(s,d) = natural(s,d)                                                     // ground = the hill; the bore is render-side
```
where `w(d)=smoothstep(0,BLEND_W,|d|−SHOULDER)`. **Pinned** `SHOULDER=4 m`, `BLEND_W=80 m`, `BALLAST_DROP=0.4 m`, `VIADUCT_THRESH=18 m`, `TUNNEL_THRESH=12 m`.
- **Normal spans:** near the track `terrainHeight≈formation+ballast` (cutting/embankment meet the bed); the track ribbon sits on it.
- **Viaduct spans:** the ballast-corridor clamp is **suppressed** — the ground is the natural valley everywhere, dropping away under the line; the **track ribbon stays at `formationHeight`**, carried on a **separate viaduct deck + piers** (render-side §2.6g) down to the valley floor (`terrainHeight` under each pier).
- **Tunnel spans:** the ground is the natural hill (above `formation`); the **track ribbon stays at `formationHeight`** inside a **bore** — render-side, the terrain-ground ribbon's near-track corridor (`|d| < BORE_HALF`, pinned `BORE_HALF=6 m`) is **omitted** and a dark bore tube + portals are drawn so the line is visible through the headland.
- **O8 is conditional:** *outside* viaduct/tunnel bands, `|d|≤SHOULDER ⇒ terrainHeight==formation+ballast`; *inside* them `terrainHeight==natural` (the divergence that makes the set-pieces possible).

**Macro vs noise (Codex MAJ4, D24):** authored macro-relief is a **flat-bottomed plateau + `SHOULDER_RAMP` smoothstep shoulder** (pinned `SHOULDER_RAMP=120 m`): a trough of `valleyDepth` per `viaducts[].center±halfLen`, a `hillHeight` bump per `tunnels[]`. **Predicates threshold on `macroReliefAt` (macro only, no noise)**, and the **set-piece structures are placed from `route.viaducts[]`/`tunnels[]` data directly** (not by scanning a noisy field). The value noise is **bounded**: `|valueNoise| ≤ NOISE_MAX` (pinned `NOISE_MAX=6 m`), and `NOISE_MAX < VIADUCT_THRESH=18`/`TUNNEL_THRESH=12` with margin, so noise can **never flip a predicate** (O9 tests the bound). The noise still rolls the *ground* between set-pieces; it just can't conjure or erase a structure.

**`naturalGround`** = `macroGround` + a seeded integer-lattice value noise (mulberry-style hash on `terrainSeed + (⌊s/CELL⌋,⌊d/CELL⌋)`, bilinear, 2–3 octaves, amplitude-clamped to `NOISE_MAX`) — pure, banned-token-free. **`anchorY`** is a tested invariant (O7); **`clampEye`** floors the eye (O20, `MIN_EYE_CLEARANCE=0.5 m`).

### 2.3 `src/sim/environment.ts` (CHG, PURE, TESTED) — realism palette + ACES re-tune
Pure `{time × weather}` additions: `exposure`, `sunDir` (unit), `sunColorPbr`, `bloomStrength`. **ACES is a palette re-tune (D14):** existing emissives were tuned for `NoToneMapping`; ACES desaturates/compresses, so emissives + per-time `exposure` are re-balanced and screenshot-checked (ENV4 night×rain before/after). ENV1–ENV6 shapes unchanged; the new fields are pinned by **O10–O12 in `test/environment.test.ts`**.

### 2.4 `src/render/quality.ts` (CHG, PURE, TESTED) — tiers, LOD, budgets (pinned)
```ts
export interface QualitySettings { pixelRatioCap:number; rainCount:number;
  shadowsEnabled:boolean; bloomEnabled:boolean; ribbonHalfWidth:number; terrainSegLen:number; terrainSubdiv:number; attitudeScale:number; }
export function qualityFor(env): QualitySettings;
export function lodForDistance(distance: number): 0|1|2;   // tier-INDEPENDENT (LOD1_DIST=120, LOD2_DIST=350)
export function ribbonVertexCount(tier: QualitySettings, routeLength: number): number;
```
| tier | shadows | bloom | ribbonHalfWidth | terrainSegLen | terrainSubdiv | attitudeScale |
|---|---|---|---|---|---|---|
| desktop | on | on | 180 m | 8 m | 24 | 0.35 |
| coarse-pointer | off | off | 110 m | 20 m | 10 | 0 |
| reducedMotion | (unchanged) | (unchanged) | — | — | — | 0 |
- Existing `{pixelRatioCap, rainCount}` preserved; `reducedMotion` ⇒ `rainCount=0` & `attitudeScale=0`.
- `ribbonVertexCount = ceil(len/terrainSegLen + 1)·(terrainSubdiv + 1)`; budgets `HIGH_BUDGET=80 000`, `LOW_BUDGET=25 000` (O18).
- **`O-RIBBON` (ribbon/curve invariant, scoped — finding #7):** `ribbonHalfWidth ≤ 0.5·minCurveRadius(route)` **for terrain-rendered routes** (i.e. `KINGSGATE_SEAHAVEN`); with desktop `ribbonHalfWidth=180` this requires `minCurveRadius ≥ 400 m`, so the **grand route's** curvature floor is 400 m. **`WESTFORD_EASTBANK` is exempt** (a physics/signalling fixture, never terrain-rendered, keeps its 250 m curve).

### 2.5 `src/sim/route.ts` (CHG, data) — the Grand Route
`KINGSGATE_SEAHAVEN`, ~**14 000 m**, 7 chapters; optional `viaducts?/tunnels?/terrainSeed?`. Lookups unchanged.

| # | Chapter | Chainage (m) | Set-piece |
|---|---|---|---|
| 1 | **Kingsgate** (city terminus) | 0 – 1 800 | Platforms, station roof, city blocks + OLE; shallow **cutting**. |
| 2 | **Ashcombe** (suburb) ~2 000 | 1 800 – 4 200 | Terraced backs, overbridge, 1.0 % climb onto a low **embankment**. |
| 3 | **Wealdham** (country) ~5 800 | 4 200 – 7 500 | Fields, tree clumps, sweeping curve (κ≈1/800, **R=800 m**) — the line **bends**. |
| 4 | **Wyre Viaduct** (no stop) | 7 500 – 8 600 | `{center:8050, halfLen:320, valleyDepth:45}`; deck + piers to the valley floor; river plane; **placed via `placeOnCentreline`**, on a κ=0 span. |
| 5 | **Brinemouth** (coast) ~9 800 | 8 600 – 11 000 | Falling grade to sea; **sea plane** one side; wide shore curve (R≥400). |
| 6 | **Stonehead Tunnel** (no stop) | 11 000 – 12 400 | `{center:11700, halfLen:380, hillHeight:60}`; hill ground + bore cutout + portals; on a κ=0 span. |
| 7 | **Seahaven** (sea terminus) | 12 400 – 14 000 | Seaside station, lamps, people; low embankment to the buffers. |

~6 stations; signals = station starters; **curvature radii ≥ 400 m**. Invariants O19; **O19b: viaducts/tunnels bands on κ=0 segments** (geometric reason — straight piers / straight bore axis; `placeOnCentreline` lands them correctly on curves regardless — finding #3/#4-r2).

### 2.6 Render adapters (IMPURE, screenshot-verified) — no spatial math
- **`src/render/terrain-mesh.ts` (NEW).** Build ONCE on a single unified `(s,d)` grid (`placeOnCentreline(s,d)`, Y `terrainHeight(s,d)`). **Seam contract:** central-difference normals on the unified grid FIRST, then slice into `RIBBON_SECTIONS=ceil(length/SECTION_LEN)` (pinned `SECTION_LEN=900 m`) sections sharing boundary verts+normals identically (cull independently, no popping). **In tunnel bands omit the near-track corridor `|d|<BORE_HALF`** (the bore replaces it). PBR + procedural textures; static **far-horizon band** behind; fog hides the edge. A separate **track ribbon** (rails/sleepers/ballast) runs along `placeOnCentreline(s,0)` at `formationHeight(s)` — always, including the viaduct deck and the bore floor.
- **`src/render/scene.ts` (CHG).**
  - **(a) Camera/cab:** `eyePose` → `camera.position`; the impure layer eases `(pitch,roll)` toward `cabAttitudeTarget` and sets `camera.rotation.set(easedPitch + look.pitch, heading + π + look.yaw, easedRoll)` (YXZ — the **`+π` faces +Z**, Codex CRIT2); `cabMount` takes the same eased pitch/roll with `heading + π`, no look.
  - **(b) Lineside via `placeOnCentreline`:** rails/sleepers swept; **signal heads, stations, masts, fences, mileposts, contact wire, AWS markers** positioned at `placeOnCentreline(chainage, sideOffset)`; **each mesh gets its Three facing offset** (front-+Z meshes use `heading`; signal lamps/boards use `heading+π` to face the approaching train).
  - **(c) Rain** anchored to the eye `(x,y,z)`.
  - **(d) Tone-mapping (D17):** `renderer.toneMapping=ACESFilmicToneMapping`, `renderer.outputColorSpace=SRGBColorSpace` set once. No-composer (mobile) path applies ACES on the canvas render. Composer path: `RenderPass(linear)→UnrealBloomPass(linear HDR)→OutputPass`, where `OutputPass` reads `renderer.toneMapping` and applies ACES+sRGB once. `toneMappingExposure=env.exposure`.
  - **(e) Sky + IBL (D23):** a gradient **sky dome** `material.fog=false`, zenith = render-local darkening of `env.skyColor`. A **tiny 16×16 procedural equirect** env texture with **`mapping=EquirectangularReflectionMapping`** and **`colorSpace=SRGBColorSpace`** assigned to `scene.environment` (auto-PMREM'd) so `envMapIntensity` is meaningful — wet-rail sheen. **Texture color-spaces pinned:** albedo/emissive/env canvas textures `SRGBColorSpace`; normal/roughness data textures `NoColorSpace`. The env texture is rebuilt/disposed safely on environment change.
  - **(f) Shadows:** one **eye-tracking** shadow `DirectionalLight` along `env.sunDir`: ±100 m ortho frustum moved with the eye each frame, 2048² map (desktop), tuned bias/normalBias; gated by `shadowsEnabled`.
  - **(g) Bloom load (D22):** `createScene` stays **synchronous**. When `bloomEnabled`, it kicks off a lazy `import()` of the composer/bloom modules and **renders direct-to-canvas (ACES on the renderer) until the promise resolves**, then switches to the composer. On coarse/mobile (`bloomEnabled=false`) the import is never started (S5 asserts the chunk isn't requested).
  - **(h) Set-pieces:** the **viaduct** (deck along `placeOnCentreline` at `formationHeight`, instanced **piers** dropped to `terrainHeight` at each pier's `(s,d)`, arch spans, a river plane at the valley trough) and **tunnel** (portals + dark bore tube around `placeOnCentreline(s,0)` at `formationHeight`) — both follow the bent spine. Flat ground deleted.
- **`src/render/scenery.ts` (CHG).** Delete `addBank`+cone-hills; props' `(x,z,heading)` from `placeOnCentreline`, Y from `anchorY`; detail by `lodForDistance`.
- **`src/render/textures.ts` (NEW).** Canvas-2D albedo+normal maps + the tiny env gradient, color-spaces per (e). No committed image assets.
- **`src/render/cab.ts` (CHG, tiny).** Mount takes eased pitch+roll.
- **`src/main.ts` (CHG, tiny).** Grand Route; quality opts; realism params; eye pose.
- **`scripts/check-dist.mjs` (NEW).** Post-build gate (O22).

### 2.7 What stays pure / untested
- **Pure + tested:** `centerline.ts`, `terrain.ts`, `camera.ts`, `environment.ts`, `route.ts` data, `quality.ts`.
- **Untested impure adapters (named, screenshot-verified):** `terrain-mesh.ts`, `textures.ts`, `scene.ts`, `scenery.ts`, `cab.ts`, `main.ts`. Zero-alloc is structural (build-once + reused rain buffer) — code review + heap-snapshot smoke (O21 demoted).
- **Untouched:** `physics.ts`, `simulation.ts`, `controls.ts`, `aws.ts`, `train.ts`, `ui/*`, `audio/*`, `input/*`, `motion.ts`.

---

## 3. File-level impact map

| File | Action | pure/impure | Why |
|---|---|---|---|
| `src/sim/centerline.ts` | **NEW** | pure (tested) | path: `centerlineAt/heightAt/headingAt/planarPoseAt/placeOnCentreline`. |
| `src/sim/terrain.ts` | **NEW** | pure (tested) | two-surface `formationHeight`/`terrainHeight`, macro relief + predicates, bounded noise, `anchorY`, `clampEye`. |
| `src/sim/camera.ts` | **NEW** | pure (tested) | `eyePose`, `cabAttitudeTarget` (imports centerline+terrain — resolves the cycle). |
| `src/sim/environment.ts` | **CHG** | pure (tested) | realism knobs; ACES re-tune; O10–O12. |
| `src/sim/route.ts` | **CHG** | pure (tested) | `KINGSGATE_SEAHAVEN` + optional bands; radii ≥ 400 m; `WESTFORD_EASTBANK` kept (fixture, exempt from O-RIBBON). |
| `src/render/quality.ts` | **CHG** | pure (tested) | tier + tier-independent `lodForDistance` + `ribbonVertexCount` + scoped `O-RIBBON`. (G3 glob widened.) |
| `src/render/terrain-mesh.ts` | **NEW** | impure | Two-ribbon (ground + track), sectioned/seam-welded, bore cutout, far-horizon band. |
| `src/render/textures.ts` | **NEW** | impure | Procedural textures (+color-spaces) + tiny equirect env map. |
| `src/render/scene.ts` | **CHG** | impure | eyePose + eased attitude + `+π` yaw; lineside + set-pieces via `placeOnCentreline`+facing; ACES once; async bloom; tiny env map; eye-tracking shadow; viaduct/tunnel; delete flat ground. |
| `src/render/scenery.ts` | **CHG** | impure | Delete fakes; place props via `placeOnCentreline`+`anchorY`; LOD. |
| `src/render/cab.ts` | **CHG (tiny)** | impure | Eased pitch+roll. |
| `src/main.ts` | **CHG (tiny)** | impure | Grand Route; quality; realism; eye pose. |
| `scripts/check-dist.mjs` | **NEW** | — | Post-build asset/size gate (O22). |
| `test/centerline.test.ts` | **NEW** | — | O1–O6, O5b/O5c/O5d, O16. |
| `test/terrain.test.ts` | **NEW** | — | O7–O9, O8-cond, O20. |
| `test/world.test.ts` | **NEW** | — | O13–O15, O17–O19/O19b, O-RIBBON. |
| `test/environment.test.ts` | **CHG** | — | O10–O12. |
| `test/reach.test.ts` | **CHG** | — | QUAL `toEqual` updated for new fields. |
| `package.json` | **CHG** | — | 0.10.0 → 0.11.0; document `build && node scripts/check-dist.mjs`. |
| `src/sim/{physics,simulation,controls,aws,train}.ts`, `ui/*`, `audio/*`, `input/*` | **UNCHANGED** | — | No physics/gameplay/HUD/audio/input change. |

---

## 4. Acceptance criteria (numbered oracles — definition of done)

**Pure unit oracles (vitest, Node, no GPU):**
- **O1–O3** height: analytic integral ≤1e-9; differential vs `gradeAt` ≤1e-6 off breakpoints; C0 ≤1e-9.
- **O4** tangent G1 ≤1e-6 across every curvature breakpoint; `‖tangent‖≈1`.
- **O5** single-arc closed form (ψ₀=0); **O5b** two-arc compounded pose vs hand-computed (≤1e-6, catches rotate-by-ψ₀).
- **O5c** `placeOnCentreline(s,d)` exactly `|d|` from the spine, perpendicular — **including the left/right sign**: on a straight (heading=0), `d=−0.5 ⇒ x=−0.5` (catches mirrored `d`, finding #9).
- **O5d** heading semantics: straight ⇒ `heading=0` (tangent=+Z), `z(s)=s`; the camera-yaw convention `heading+π` is documented and exercised by a render-side assertion (a fixed straight chainage ⇒ camera looks toward +Z).
- **O6** cant non-tautological (below-clamp equals the formula; over-clamp equals `CANT_MAX`; sign).
- **O7** anchor: `anchorY−terrainHeight===clearance`, `anchorY≥terrainHeight`.
- **O8 (conditional)** **outside** viaduct/tunnel bands, `|d|≤SHOULDER ⇒ terrainHeight==formation+ballast` (≤1e-9, exact on curves); **inside** a viaduct or tunnel band, `terrainHeight(s,d)===naturalGround(s,d)` (the ground diverges from the deck) — the two-surface invariant (D20).
- **O9** macro predicates + bounded noise: with the plateau+`SHOULDER_RAMP` profile, `macroReliefAt ≈ ±depth` flat across each band and `|macroReliefAt|<THRESH` beyond `SHOULDER_RAMP` of the edge ⇒ predicates true on the band, **false ≥120 m outside**, by construction; `|valueNoise| ≤ NOISE_MAX (6) < THRESH` so noise never flips a predicate; predicates never both true; `terrainHeight` bit-identical on repeat; `terrainSeed` change alters the *ground* but **not** any predicate (tested).
- **O10–O12** (env): exposure/bloom ordered; `|sunDir|=1`; pure, finite.
- **O13** tier ordering; **O14** LOD exact step (120/350, tier-independent); **O15** quality domain sweep.
- **O16** cab attitude pinned + non-tautological (synthetic over-clamp exercises the ceilings; `scale=0 ⇒ {0,0}`).
- **O17** layering guard import-scoped (parse import specifiers; physics/simulation reach neither centerline/terrain/camera); G3 (comment-stripped) passes over `src/sim/*.ts` + `src/render/quality.ts`.
- **O18** ribbon vertex budget numeric; **O19** route invariants (radii ≥ 400 m for `KINGSGATE_SEAHAVEN`); **O19b** set-pieces on κ=0; **O20** eye clearance.
- **O-RIBBON** `ribbonHalfWidth ≤ 0.5·minCurveRadius` for terrain-rendered routes (`WESTFORD_EASTBANK` exempt).

**Build + screenshot gates:**
- **O21 (demoted)** zero-alloc structural; **O22** static-host build-gate (`check-dist.mjs`); **REG** 164 tests pass; `tsc` clean; build clean; **ENV4** μ=0.20 route-independent (visual ACES re-tune screenshot-checked).
- **S1** cutting + embankment; **S2** the viaduct carries the line on arches **over a visibly open valley with the river below** (the deck high above the ground — proves the two-surface model) + the tunnel portal/bore with the line visible through it; **S3** rails + props + set-pieces follow the curve, cab gently banked, **camera faces forward (not backward)**; **S4** ACES re-balanced, crisp eye-tracking shadows, gradient sky above the horizon band, lamp bloom, wet-rail env sheen; **S5** mobile gate: renders, zero errors, shadows/bloom off, attitude flat, coarser ribbon, **bloom chunk not requested**, train drives; **S6** desktop zero errors, train drives the bent line, driver on the left, no cab-furniture clip.

---

## 5. Residual risks
- **R1** Aesthetic quality + ACES re-tune need human eyes (S1–S4). **R2** Perf on 14 km (sectioned ribbon + gates; FPS sanity). **R3** Cab comfort (bounded, tunable). **R4** `three/examples` under Vite `base "./"` (fallback = direct ACES + no bloom). **R5** ACES re-tune effort (top schedule risk). **R6** `z(s)≤chainage` on curves (drive from `centerlineAt`). **R7** Tunnel interior cheap (fog+portal). **R8** Value-noise tuning. **R9** Eye-tracking shadow bias tuning. **R10** Tiny-env-map sheen reads as a tint, not a mirror (acceptable, S4).
- **R11 (NEW)** The two-surface viaduct/tunnel is the trickiest render piece: the deck/pier and bore/portal geometry must align with `formationHeight` and the diverging `terrainHeight` exactly at the band edges (where the ground transitions from "==formation near track" to "natural"). Screenshot-gated (S2); the `SHOULDER_RAMP` shoulder gives a smooth visual handoff at the band edge.

## 6. Decision log
*(D1–D19 carry from V3 unless noted.)*
- **D1''** **`camera.ts` restored as its own module** (reversing V2's D1' fold): folding the camera helpers into `centerline.ts` created an ESM cycle (`cameraFrame`→`clampEye` in `terrain`→`heightAt` in `centerline`). Split: `centerline` path-only, `terrain` imports `centerline`, `camera` imports both. *(Codex MAJ3.)*
- **D20 (CRITICAL fix) — two-surface terrain.** Separate `formationHeight` (the track/deck the train always runs on) from `terrainHeight` (the ground). They coincide near the track on normal spans, but in **viaduct** bands the ground = the natural valley (the deck is carried separately on piers) and in **tunnel** bands the ground = the hill (the bore is a render-side cutout + shell). A single heightfield could represent neither (it filled the valley / couldn't bore the hill). O8 is conditional. *(Codex CRIT1 — the headline external fix.)*
- **D21 (CRITICAL fix) — camera/Three yaw convention.** `placeOnCentreline`/`headingAt` return the *mathematical* heading (heading=0 ⇒ tangent=+Z). The render layer applies the Three facing offset: camera `rotation.y = heading + π + lookYaw` (Three cameras look down −Z, so +π faces +Z — matching today's `BASE_YAW=π`); front-+Z meshes use `heading`; rear-facing signal lamps use `heading+π`. V3 omitted the +π and would have pointed the camera down the line backwards. *(Codex CRIT2.)*
- **D22 — async bloom over a sync scene.** `createScene` stays synchronous; on `bloomEnabled` it lazy-`import()`s the composer and renders direct-to-canvas (ACES on the renderer) until ready, then switches. Mobile never starts the import (S5). *(Codex MAJ6.)*
- **D23 — color-space + env-map mapping pinned.** Albedo/emissive/env canvas textures `SRGBColorSpace`; normal/roughness data `NoColorSpace`; the env texture `mapping=EquirectangularReflectionMapping` (else PMREM won't treat it as an env map); rebuilt/disposed safely on env change. *(Codex MAJ5.)*
- **D24 — predicates on macro relief; bounded noise.** `viaductSpanAt`/`tunnelBoreAt` threshold on `macroReliefAt` (no noise); set-piece structures are placed from `route.viaducts[]`/`tunnels[]` data; `|valueNoise| ≤ NOISE_MAX (6 m) < THRESH` so noise can't flip a predicate (O9). Renders the set-pieces from data, not by scanning a noisy field. *(Codex MAJ4.)*
- **D25 — `O-RIBBON` scoped to terrain-rendered routes.** The `ribbonHalfWidth ≤ 0.5·minCurveRadius` invariant (and the 400 m floor) applies to `KINGSGATE_SEAHAVEN`; the retained `WESTFORD_EASTBANK` fixture (250 m curve, physics/signalling only, never terrain-rendered) is exempt. *(Codex MAJ7.)*
- **D26 — `lateralOffsetAt` removed; G3 comment-stripped; O5c gains a sign assertion.** The path model is an accumulated `(x,z,heading)` pose (`planarPoseAt`), not a single offset; G3 strips comments before matching; O5c pins the left/right sign. *(Codex minors #8/#9/#10.)*
